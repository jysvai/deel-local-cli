// 사용자가 무엇을 시켰는지 보고 알맞은 작업 모드를 고른다.
//
// 왜 규칙으로 하나:
//   모델에게 "이건 무슨 일이냐" 고 한 번 더 물을 수도 있다. 그런데 그러면
//   한마디 할 때마다 왕복이 하나 늘고, 작은 로컬 모델은 이런 분류를 잘 못한다.
//   느려지고 틀리기까지 하면 자동으로 하는 의미가 없다.
//
//   그래서 낱말로 고른다. 대신 두 가지를 지킨다.
//     1) 애매하면 안 바꾼다. 종합 모드에 그대로 둔다.
//        잘못 바꾸는 것보다 안 바꾸는 편이 낫다 — 특히 읽기 전용 모드로
//        잘못 보내면 사용자는 "왜 파일을 안 고쳐?" 하고 막힌다.
//     2) 왜 그렇게 봤는지 남긴다. 화면에 그 이유가 같이 뜬다.
//
// 사용자가 직접 모드를 고르면 이건 아예 안 돈다. 사람이 고른 것을 뒤집지 않는다.

// 낱말과 점수. 큰 점수일수록 그 모드라는 증거가 강하다는 뜻이다.
//
// 한국어를 먼저 두되 영어도 같이 본다 — 오류 메시지를 그대로 붙여넣는 일이 흔하다.
/*
 * 검사가 이 표를 그대로 읽는다 (test/자동모드.test.js).
 *
 * 규칙을 넣기만 하고 그게 실제로 걸리는지는 아무도 안 본다 — 안 걸리는 규칙은
 * 없는 것과 같고, 그런데도 화면은 멀줦하다(다음 규칙이 받아 그럴듯한 모드를 낸다).
 * 그래서 밖으로 내놓고, 규칙 하나하나가 실제 문장에 걸리는지를 재게 한다.
 */
/*
 * ── 「… 좀 해줘」 의 빈칸 ────────────────────────────────────────────────
 *
 * 낱말 뒤에 붙는 토씨를 `점검(해|좀|을|이)` 처럼 **붙여서만** 봤다. 그런데
 * 한국말에서 부탁할 때는 그 사이가 거의 언제나 벌어진다 —
 *
 *   "점검 좀 해줘" · "설명 좀 해줘" · "수정 좀 해줘" · "구현 좀 해줘"
 *
 * 이 꼴이 전부 **하나도 안 걸려서** 종합 모드에 그대로 남았다. 규칙이 안
 * 걸리면 아무 데서도 안 터지므로(점수만 안 오른다) 여태 아무도 몰랐다.
 * 자동모드.test.js 의 죽은 규칙 검사가 「이 규칙에 걸리는 실제 문장이 없다」
 * 로 잡아 줬다.
 *
 * 그래서 '좀' **앞에만** 빈칸을 열어 둔다(`\s*좀`). 토씨 전체에 열면
 * "점검 이력" 이 「점검 이」 로 걸리는 식으로 넓어진다 — 가르는 자리가
 * 미묘해서, 여는 것도 좁게 연다.
 */
/*
 * ── 영어 시킴말 ─────────────────────────────────────────────────────────
 *
 * 여기 한 번만 적고 두 자리에서 쓴다 — 아래 code 점수표와, 파일 맨 아래의
 * 손대라했나. 두 벌로 두면 한쪽만 늘어나고, 실제로 그렇게 어긋나 있었다.
 *
 * 손대라했나 는 build·create·make·develop·scaffold 를 다 알았다. 그런데
 * code 점수표의 영어는 `implement|add|fix|write` 넷이 2점씩인 것과, 따로 선
 * `rename` 3점이 전부였다. code 문턱은 3이라 **2점짜리 하나로는 절대 못
 * 넘는다** — 무늬가 아는 동사 스무 개를 재 보니 `rename` 하나만 넘고,
 * 저 넷이 2점, 나머지 열다섯이 0점이었다.
 *
 *     "Build a complete work request and approval web application."
 *       손대라했나 → 참        code 점수 → 0
 *
 * 그래서 영어로 시킨 일은 무슨 말이든
 * 종합에 남았고 — 그것만이면 탈이 없다 — **긴 명세에서는 졌다.** 지나가던
 * `error`·`failed` 가 6점이라, 만들라는 3,577자 지시문이 디버그로 갔다.
 * 2차 리뷰가 「설계 낱말이 없는 명세는 그대로 디버그로 간다」 고 짚은 자리가
 * 이것이고, 증상이 아니라 이쪽이 뿌리였다.
 *
 * 점수를 맞춰 두면 1·2등이 붙어서, 원래 있던 「비기면 안 고른다」 규칙이
 * 알아서 받아 낸다. 새 울타리를 하나 더 세우는 것보다 이쪽이 낫다.
 *
 * 넓히되 자리는 그대로다. 첫머리에서만 본다 — 문장 가운데 낱말은 시킴말이
 * 아니다. 「the build is broken」 이 code 로 가면 넓힌 것이 아니라 망가뜨린
 * 것이다. 번호·목록 표시 뒤도 첫머리로 친다 — 붙여 넣는 명세가 그 꼴이다.
 *
 * ── 어디까지가 「첫머리」 인가 ───────────────────────────────────────────
 *
 * 처음엔 `[.!?
]\s*` 였다. 마침표 뒤 **빈칸 0개**도 문장 첫머리로 친다는
 * 뜻이었고, 3차 리뷰가 그 자리에서 제일 나쁜 것을 찾아냈다 —
 *
 *     "Explain user.delete in detail."   →  code 모드
 *
 * `user.delete` 의 `delete` 가 시킴말이 되고, 설명해 달라는 말이 파일을
 * 고치는 모드로 갔다. 묻기 점수는 4점이나 있었는데 「고치라는 말」 로 잡혀
 * 후보에서 빠졌다 — 이 파일 맨 위가 걱정한 바로 그 모양이다.
 *
 * 그래서 셋을 죈다.
 *   · 마침표 뒤에 **빈칸이 하나는 있어야** 다음 문장이다.
 *   · 홑글자 뒤의 마침표는 문장 끝이 아니라 **약자**다 (`e.g. create`).
 *   · 시킴말 뒤에 실패말이 오면 그건 목적어가 아니라 **주어**다. 영어
 *     시킴말 뒤에는 목적어가 오지 과거분사가 오지 않는다 —
 *     `Build failed with exit code 1.` 은 붙여 넣는 빌드 오류의 첫 줄이고,
 *     그게 code 로 가면 고장 신고가 만들기 지시가 된다.
 *
 * 글 처음의 들여쓰기는 받아 준다(`^\s*`). 번호·목록 갈래는 처음부터
 * 그랬는데 맨낱말 갈래만 안 그래서, 빈칸 두 칸으로 시작하는 지시문이
 * 통째로 0점이었다.
 *
 * 정규식 리터럴로 적는다. 글자열을 이어 붙여 new RegExp 에 넣으면 홑따옴표
 * 안에서 역빗금이 사라지고 낱말 경계가 백스페이스 글자가 된다 — 검사에서
 * 잡히기 전까지 조용히 아무것도 안 맞는다. 실제로 그렇게 났다.
 *
 * ── 약자 목록에는 「뒤에 말이 이어지는 것」만 ───────────────────────────
 *
 * 8차 리뷰가 셋을 짚었고, 같은 부류를 하나 더 찾아 넷을 뺐다. 약자 목록을
 * 넓히면서 **문장을 맺는 자리에 더 자주 오는 말**을 같이 넣어 버렸다.
 *
 *     "No. Fix the bug."                        → `No.` 가 약자
 *     "Install git, node, etc. Build it."       → `etc.` 가 약자
 *     "Ship it to Acme Inc. Create the report." → `Inc.` 가 약자
 *     "Sold to Acme Ltd. Build the invoice."    → `Ltd.` 가 약자
 *
 * 넷 다 마침표가 진짜 문장 끝인데 약자로 읽혀 **뒤 문장이 통째로 죽었다.**
 *
 * 남길 것과 뺄 것을 가르는 것은 **그 마침표 뒤에 무엇이 오느냐**다.
 * `e.g.` · `i.e.` · `Fig.` · `Dr.` · `Mr.` 는 뒤에 말이 이어지는 자리에 오는
 * 약자라 마침표를 문장 끝으로 읽으면 안 된다. `No.` · `etc.` · `Inc.` ·
 * `Ltd.` 는 반대로 **문장을 맺는 자리**에 훨씬 자주 온다 — 지키려던 것보다
 * 잃는 것이 크면 그건 울타리가 아니라 벽이다. 그래서 뺀다.
 *
 * ── 공손말에 쉼표가 붙는다 ──────────────────────────────────────────────
 *
 * `please\s+` 는 빈칸만 받았다. 그런데 영어에서 제일 흔한 공손꼴이
 * `Please, could you …` 다 — 쉼표가 하나 붙었다고 0점이 됐다.
 * `please\s*,?\s+` 로 쉼표를 받아 준다. `could you` 뒤도 마찬가지다.
 *
 * ── 2번 갈래의 실패말은 로그꼴일 때만 막는다 ────────────────────────────
 *
 * 실패말 차단 목록이 두 갈래에서 달랐다. 1번 갈래(`build`·`create`…)는
 * 이름씨꼴(`errors` · `failure` · `crash` · `timeout`)까지 막는데, 2번
 * 갈래(`delete`·`rename`·`change`…)는 움직씨꼴만 막았다. 그래서
 *
 *     "Delete error: permission denied"   → 시킴말
 *
 * 같은 로그 첫 줄이 지시로 읽혔다. 그렇다고 2번 갈래에 이름씨꼴을 그냥
 * 넣으면 안 된다 — `Fix errors.` · `Fix error in auth module.` 은 **진짜
 * 지시**다. 1번 갈래와 달리 2번 갈래의 낱말들은 그 이름씨를 목적어로 받는다.
 *
 * 갈라 주는 것은 **뒤에 오는 쌍점·여는 괄호**다. 로그는 `error:` ·
 * `crash (core dumped)` 꼴로 적히고, 지시문은 그렇게 안 적힌다.
 * 그래서 2번 갈래의 이름씨꼴 차단은 `[:(]` 가 뒤따를 때만 건다.
 * 그 목록에서 `broken` 이 빠져 있었다(9차 리뷰) — 1번 갈래는 아는 낱말인데
 * 여기만 몰라서 `Rewrite broken: exit status 1` 이 지시로 읽혔다.
 *
 * ── 동사 뒤에 쌍점이 오면 그건 꼬리표다 ─────────────────────────────────
 *
 * 리뷰를 기다리는 사이에 붙여 넣을 법한 글 서른여덟 줄을 죽 재 봤다.
 * 일곱이 시킴말로 읽혔고, 다섯이 한 가지 모양이었다.
 *
 *     "fix: 값이 아닌 것을 가렸습니다"      ← 이 저장소의 커밋 메시지다
 *     "Change-Id: I8f2c1a"                git 꼬리표
 *     "Remove-Item : Cannot find path"    파워셸 오류
 *     "Update available: 1.2.3 → 1.3.0"   npm 알림
 *     "add(a, b) { return a + b; }"       코드 한 줄
 *
 * 시킴말 뒤에는 **목적어**가 온다. 쌍점이 오면 그건 꼬리표고, 여는 괄호가
 * 바로 붙으면 그건 부르는 짓이다. 위의 실패말 목록과 같은 생각인데,
 * 저쪽은 「어떤 낱말이 오나」 를 보고 이쪽은 「어떤 글자가 오나」 를 본다.
 *
 * 쌍점은 **첫 낱말 바로 뒤**일 때만 꼬리표다. `Fix the bug: it is in the
 * parser.` 처럼 목적어를 끼고 오는 쌍점은 그냥 지시문이다.
 *
 * ── 붙여 넣는 것이 로그만은 아니다 ─────────────────────────────────────
 *
 * SQL 도 시킴꼴로 시작한다.
 *
 *     delete from users where id = 1;
 *     update accounts set balance = 0 where id = 3
 *
 * 「이 쿼리가 왜 이러냐」 고 물으려고 붙였는데 파일을 고치는 모드로 갔다.
 * 손대라는 말로 읽히면 읽기 전용 모드가 후보에서 통째로 빠진다.
 *
 * 갈라 주는 것은 `from` 이 아니라 **뒤에 SQL 이 이어지느냐**다.
 * `Delete from the list.` 는 그대로 지시문이고, `delete from users where`
 * 는 아니다. 두 동사(`delete`·`update`)만 우리 목록과 겹치므로 그 둘에만
 * 건다 — `insert`·`select` 는 애초에 여기 없다.
 *
 * ── 이음말 뒤도 첫머리다 ───────────────────────────────────────────────
 *
 * 같은 자리에서 반대쪽 구멍이 하나 나왔다.
 *
 *     "First, set up the project. Then add tests."   → 0점
 *
 * 붙여 넣는 명세가 딱 이 꼴인데 `First,` · `Then` 이 앞에 있다고 첫머리가
 * 아니게 됐다. 번호·목록 표시를 첫머리로 치는 것과 같은 까닭으로 받는다.
 * 이음말 뒤가 동사가 아니면(`Now the build is broken.`) 그대로 안 걸린다.
 *
 * ── 만들 것이 계획이면 그건 계획이다 ────────────────────────────────────
 *
 * 같은 뜻을 한국어·영어로 나란히 스물여섯 짝 재 보다 찾았다.
 *
 *     "계획 세워 줘"            → plan
 *     "Make a plan for this."   → **code**
 *
 * 시킴꼴인 것은 맞다. 그런데 이 규칙이 손대라했나 의 영어 쪽이기도 해서,
 * 걸리는 순간 읽기 전용 모드(plan·architect·ask·inspect)가 **후보에서
 * 통째로 빠진다.** 계획을 달라는 말에 계획 모드가 남을 수가 없었다.
 *
 * 갈라 주는 것은 동사가 아니라 **목적어**다. 만들 것이 계획·로드맵·
 * 전략이면 그건 계획 모드가 내놓는 것이지 파일을 고쳐서 되는 일이 아니다.
 *
 * ── 검사에 쓰던 낱말이 목록에 없었다 ────────────────────────────────────
 *
 * 8차 리뷰를 재 보다 찾았다. 검사 쪽이 `Rewrite what is left of the parser.`
 * 를 **쓰기 지시문의 보기**로 쓰고 있는데, 정작 `rewrite` 가 목록에 없어
 * 0점이었다. `write` 는 있지만 낱말 경계 때문에 `rewrite` 안에서는 안 걸린다.
 *
 * 0점이면 「읽기 점수가 안 붙는다」 가 언제나 참이라, 그 검사는 이 낱말에
 * 대해 아무것도 안 재고 있었다. 고치는 것은 있는 자리에 넣는 일이다 —
 * 다시 쓰는 것은 있는 것을 고치는 일이라
 * 「고쳐 쓰는 말」 갈래다. 처음엔 손대는 갈래에 뒀는데, 그러면 계획 울타리를
 * 못 받아 `Rewrite the plan.` 이 코드로 갔다(9차 리뷰) — `write` 와 짝이
 * 맞아야 하는 자리다.
 *
 * 짓는 갈래(1번)와 고쳐 쓰는 갈래(2번)에 건다. 3번의 `implement`·`add` 는
 * 반대다 — "Implement the plan we agreed on." 은 계획을 **실행**하라는
 * 말이고, "Add a plan field to the schema." 의 plan 은 목적어가 아니라
 * 딸린 이름씨다. 둘 다 코드가 맞다.
 *
 * ── 계획거리 뒤에 무엇이 와야 계획인가 ──────────────────────────────────
 *
 * 그 울타리를 「문장 끝 또는 전치사」 로만 열어 뒀더니, 때를 가리키는 말
 * 한 마디만 붙어도 빠져나갔다(10차 리뷰).
 *
 *     "Update the plan now."   "Create a plan today."   "Make a plan first."
 *
 * 때말 여덟과 **사이를 띄운 줄표**(`Make a plan - urgently`)를 같이 받는다.
 * 줄표는 띄웠을 때만 구분자다 — `Update the plan-file.` 은 그대로 코드다.
 * 계획거리 앞에 붙임표가 오는 꼴(`an action-plan`)도 계획거리로 친다.
 *
 * ── 실패말이 이름의 일부면 그건 로그가 아니다 ───────────────────────────
 *
 * 실패말 목록은 두 층이다. 움직씨꼴(`failed`·`crashed`)은 뒤에 아무 문장
 * 부호나 와도 로그로 치고, 이름씨꼴(`errors`·`broken`)은 1번 갈래에서만
 * 막았다. 그런데 그 이름씨 층이 마침표와 붙임표를 통째로 구분자로 쳤다.
 *
 *     "Build errors.json"        → 0점   `.json` 의 점을 문장 끝으로 읽었다
 *     "Create error-handler.js"  → 0점   붙임표를 로그 구분자로 읽었다
 *     "Update crash-report.md"   → 0점
 *
 * 이 파일 위쪽이 마침표에서 배운 것과 같다 — **점 뒤에 낱말이 붙어 있으면
 * 그건 문장 끝이 아니다.** 줄표도 사이를 띄웠을 때만 구분자다.
 *
 * 반대쪽으로는 2·3번 갈래가 쌍점과 여는 괄호만 막고 있어서, 같은 로그가
 * 어느 동사로 시작하느냐로 갈렸다.
 *
 * 그 자리를 줄표로만 메웠더니 마침표와 전치사꼴이 남았다(11차 리뷰).
 *
 *     "Rewrite broken with exit code 1"   "Modify broken."
 *
 * `broken` 은 이름씨가 아니라 **그림씨**다. `Fix errors.` 는 오류를
 * 고치라는 말이지만 `Modify broken.` 은 아무 말도 아니다 — 세 갈래에서
 * 다 1번 갈래와 같은 잣대를 받는다. 뒤에 이름씨가 오면(`Fix broken
 * tests.`) 그건 여전히 꾸밈말이라 그대로 지시문이다.
 *
 *     "Build broken - exit status 1"     → 안 걸림 (1번 갈래)
 *     "Rewrite broken - exit status 1"   → **걸림**  (2번 갈래)
 *
 * 세 갈래에 같은 「띄운 줄표」 잣대를 넣어 맞춘다.
 */
const 영어시킴말 =
  /(?:^\s*|(?<!\b(?:e\.g|i\.e|vs|cf|approx|Fig|Dr|Mr|Mrs|Ms|St|Jr|Sr))[.!?]\s+|\n\s*|^\s*\d+[.)]\s*|^\s*[-*]\s*)(?:(?:first|then|next|after\s+that|also|now|finally|second|third|lastly|afterwards?)\s*,?\s+)?(?:please\s*,?\s+)?(?:(?:could|can|would|will)\s+(?:you|we)\s*,?\s+)?(?:please\s*,?\s+)?(?:(?:build|make|create|update|write|migrate|generate|set\s*up)\b(?![\s:-]+(?:(?:failed|failing|fails|errored|broke|crashed|succeeded|succeeds|timed[\s-]+out)\b(?=\s*(?:[.,:;!?()\[\]-]|$)|\s+(?:with|at|after|in|on|to|before|during|because|due|while|when|for|since|from)\b)|(?:failures?|errors?|broken|crash|crashes|timeouts?)\b(?=(?:[ \t]*[([][^)\]\n]*[)\]])?(?:\s*(?:[,:;!?)\]]|$)|\s*\.(?!\w)|\s+-{1,2}(?:\s|$)|\s+(?:with|at|after|in|on|to|before|during|because|due|while|when|for|since|from)\b))))(?!(?:[ \t]+[\w'-]+){0,3}[ \t]+(?:[\w']+-)*(?:plans?|roadmaps?|strateg(?:y|ies)|outlines?)(?=[ \t]*(?:[!?,;:)]|\.(?!\w)|$)|[ \t]+-{1,2}(?:[ \t]|$)|[ \t]+(?:now|today|tonight|tomorrow|first|soon|asap|again)\b|[ \t]+(?:for|to|of|on|about|around|that|which|we|with|before|after|regarding|covering)\b))|(?:modify|change|develop|rewrite)\b(?![\s:-]+(?:(?:failed|failing|fails|errored|broke|crashed|succeeded|succeeds|timed[\s-]+out)\b(?=\s*(?:[.,:;!?()\[\]-]|$)|\s+(?:with|at|after|in|on|to|before|during|because|due|while|when|for|since|from)\b)|broken\b(?=(?:[ \t]*[([][^)\]\n]*[)\]])?(?:\s*(?:[,:;!?)\]]|$)|\s*\.(?!\w)|\s+-{1,2}(?:\s|$)|\s+(?:with|at|after|in|on|to|before|during|because|due|while|when|for|since|from)\b))|(?:failures?|errors?|crash|crashes|timeouts?)\b(?:\s*[:(]|\s+-{1,2}(?:\s|$))))(?!(?:[ \t]+[\w'-]+){0,3}[ \t]+(?:[\w']+-)*(?:plans?|roadmaps?|strateg(?:y|ies)|outlines?)(?=[ \t]*(?:[!?,;:)]|\.(?!\w)|$)|[ \t]+-{1,2}(?:[ \t]|$)|[ \t]+(?:now|today|tonight|tomorrow|first|soon|asap|again)\b|[ \t]+(?:for|to|of|on|about|around|that|which|we|with|before|after|regarding|covering)\b))|(?:implement|add|fix|refactor|remove|delete|rename|scaffold)\b(?![\s:-]+(?:(?:failed|failing|fails|errored|broke|crashed|succeeded|succeeds|timed[\s-]+out)\b(?=\s*(?:[.,:;!?()\[\]-]|$)|\s+(?:with|at|after|in|on|to|before|during|because|due|while|when|for|since|from)\b)|broken\b(?=(?:[ \t]*[([][^)\]\n]*[)\]])?(?:\s*(?:[,:;!?)\]]|$)|\s*\.(?!\w)|\s+-{1,2}(?:\s|$)|\s+(?:with|at|after|in|on|to|before|during|because|due|while|when|for|since|from)\b))|(?:failures?|errors?|crash|crashes|timeouts?)\b(?:\s*[:(]|\s+-{1,2}(?:\s|$)))))(?!\.\w)(?!\()(?![-\w]*[ \t]*:)(?![ \t]+[\w.-]+[ \t]*:[ \t])(?!(?<=\bdelete)\s+from\s+[\w."`]+\s*(?:where\b|order\s+by\b|limit\b|;|$))(?!(?<=\bupdate)\s+[\w."`]+\s+set\b)/im;

/*
 * 「그 계획을 **따라 하라**」 는 말. 계획을 달라는 말이 아니다.
 *
 * 한 자리에 모아 두어야 계획·플랜·로드맵 셋에 똑같이 걸린다. 흩어 적었더니
 * 한글에만 붙고 영어에는 안 붙는 자리가 났다(아래 plan 표 머리말).
 *
 * 토씨는 있어도 없어도 된다 — 「계획 따라」 · 「계획을 따라」 · 「계획에 따라」
 * 가 다 같은 말이다.
 */
const 계획말고 = '(?!\\s*(?:대로|처럼|[을를에]?\\s*따라))';
/*
 * ── 「그 계획은 **이미 승인됐다**」 도 계획을 달라는 말이 아니다 ──────────
 *
 * 아래 영어겹침 이 같은 잣대를 이미 쓴다 — 뒤에 is·was 가 오면 그 낱말은 시킴이
 * 아니라 이미 있는 것을 가리키는 말이다. 그런데 그 잣대가 **겹침 갈래에만** 붙어
 * 있었다. 점수표의 `plan` 5점은 그대로라, 승인 창만 안 뜨고 모드는 여전히 계획이었다:
 *
 *   "Our plan is approved. Open the file and make the change."  → 계획 모드
 *
 * 계획 모드에는 파일을 바꾸는 도구가 없다. 게다가 겹침을 끄면서 승인 뒤에 code 로
 * 잇는 길(repl.js 의 이어갈모드)까지 없어져, 사람은 이미 승인됐다고 적어 놓고
 * 계획서 한 장을 다시 받고 끝난다 — 고치기 전보다 나빠진 자리다.
 *
 * 한글 낱말에는 안 건다. 영어 낱말 뒤에는 한글 토씨가 붙어 오므로(`plan대로` ·
 * `roadmap에 따라`) 위 토씨 갈래는 그대로 같이 쓴다.
 */
const 이름씨꼴영어 = '(?!\\s+(?:is|was|are|were|has|have|had|looks?|seems?|got)\\b)';
const 계획말고영어 = `${계획말고}${이름씨꼴영어}`;

export const 표 = {
  debug: [
    // 고장은 신호가 뚜렷하다. 그리고 디버그는 파일을 고칠 수 있으므로
    // 잘못 골라도 막히지 않는다. 그래서 세게 준다.
    [/왜\s*안\s*(되|돼|됨)/, 5], [/안\s*(되는|돼는|됩니다|되네|돼네|돼요|되요)/, 4],
    [/작동\s*안/, 4], [/동작\s*안/, 4],
    [/에러|오류|예외|익셉션/, 4], [/버그/, 4],
    [/실패/, 3], [/터(져|졌|진다)/, 3], [/죽(어|었|는다|네)/, 3],
    // 「깨지다」 는 검사·빌드가 무너졌을 때 한국어에서 제일 흔한 말인데
    // 터지다·죽다만 있고 이것이 빠져 있었다(같은 뜻 짝 재기).
    [/깨(져|졌|진다|지네|지는|진)/, 3],
    [/멈(춰|췄|춘다|추)/, 3], [/먹통/, 3], [/이상(해|하다|한데)/, 3],
    [/재현/, 3], [/스택\s*트레이스|stack\s*trace|traceback/i, 4],
    // 'TypeError' 처럼 앞에 글자가 붙어 오는 일이 훨씬 흔하다.
    // \berror\b 로 잡으면 정작 붙여넣은 진짜 오류 메시지를 놓친다.
    [/error/i, 3], [/\bexception\b|\bcrash|\bfail(s|ed|ing)?\b|\bbroken\b/i, 3],
    [/고장/, 3], [/왜\s*이(래|러|렇게\s*나)/, 2],
    [/왜\s*(못|안)/, 3], [/뭐\s*때문(이|에)/, 3],
    // 원인을 짚으라는 말은 점검이 아니라 디버그다 — 증상이 이미 있다는 뜻이다.
    // 이게 없으면 "오류 원인 분석해줘" 가 둘 사이에서 비겨 종합에 그대로 남는다.
    [/원인(을|이|은)?\s*(?:좀\s*)?(찾|짚|파악|밝|알아|분석|살펴|봐)/, 4],
  ],

  plan: [
    // '먼저' 와 붙어야 계획이다. 그냥 '어떻게' 는 설명일 수도 있다.
    /*
     * ── 「계획대로」 는 계획을 달라는 말이 아니다 ──────────────────────
     *
     * 계획을 받아 본 사람이 다음에 치는 말이 「위 계획대로 진행해줘」 다.
     * 그런데 그 말에 '계획' 이 들어 있어서 **또 계획 모드로** 갔다. 계획
     * 모드는 파일을 고치는 도구가 없으니, 사람은 계획을 두 장 받고 파일은
     * 그대로다. 승인한 값이 통째로 사라진다.
     *
     * 대화 화면은 승인 창 바로 뒤에만 이 자리를 피한다(`이어갈모드`). 그
     * 자리를 벗어나면 대화 화면에서도 갇히고, 에디터·`deel run` 에는 그
     * 장치가 아예 없다.
     *
     * '-대로' 는 「그것에 따라 하라」 는 뜻이다. 계획을 달라는 말과 정반대다.
     *
     * ── 그런데 두 군데가 새고 있었다 ──────────────────────────────────
     *
     * 하나는 **갈래 나누기의 힘 차이**다. `플랜(?!…)|\bplan\b` 는 「(플랜에
     * 뒤보기) 또는 (plan)」 으로 읽힌다 — `|` 가 뒤보기보다 넓게 갈라서
     * 영어 낱말에는 뒤보기가 안 붙었다. 그래서 이랬다:
     *
     *     위 계획대로 진행해줘     종합   (맞다)
     *     plan대로 진행해줘        계획 ← 5점이 그대로 붙는다
     *     roadmap대로 진행해줘     계획 ←
     *
     * 다른 하나는 **적어 둔 토씨만 막은 것**이다. 뒤보기에 `에\s*따라` 만
     * 적어 뒀는데, 사람은 토씨를 흔히 뺀다:
     *
     *     위 계획 따라 진행해줘     계획 ←
     *     세운 계획을 따라 만들어줘  계획 5점 (실행 낱말 덕에 겨우 code 로 감)
     *
     * 뒤보기를 한 자리에 모아 두 낱말에 똑같이 씌운다. '따라' 앞에 오는
     * 토씨는 없거나 `에`·`을`·`를`·`대로` 다.
     */
    [new RegExp(`계획${계획말고}`), 5],
    [new RegExp(`플랜${계획말고}|\\bplan\\b${계획말고영어}`, 'i'), 5],
    [/먼저\s*(정리|생각|살펴|파악|검토|짜|세)/, 4],
    [/어떤\s*순서/, 4], [/순서(를|대로)?\s*(정|짜|잡)/, 3],
    [/단계(를|로)?\s*(나눠|쪼개|정리)/, 3],
    [new RegExp(`로드맵${계획말고}|\\broadmap\\b${계획말고영어}`, 'i'), 5],
    [/방향(을|만)?\s*(잡|정)/, 3],
    [/하기\s*전에\s*(정리|살펴|검토)/, 3],
    [/무엇부터|뭐부터/, 3],
  ],

  architect: [
    [/설계/, 5], [/아키텍(처|쳐)|architecture/i, 5],
    [/구조(를|가|는)?\s*(어떻게|바꾸|잡|짜|개선)/, 5],
    [/어떻게\s*(나누|쪼개|짜)/, 5],
    [/모듈(로|을)?\s*(나누|분리)/, 3],
    [/의존(성|\s*방향)/, 3], [/레이어|계층/, 3],
    [/어느\s*(쪽|것)이\s*(나을|좋을)/, 3], [/선택지|대안/, 3],
    [/패턴(을|이)?\s*(쓰|적용|고르)/, 2],
    [/\bdesign\b|\bstructure\b|\brefactor(ing)?\s*(방향|plan)/i, 3],
  ],

  /*
   * 점검 — 「지금 무엇이 잘못돼 있나」 를 찾는 일.
   *
   * 이 갈래가 없던 동안 이런 말이 전부 묻기(ask)로 갔다. 「설명해줘」 한 마디가
   * 5점이라 그것만으로 문턱을 넘기 때문이다. 그런데 묻기는 짧게 답하고, 얕게
   * 생각하고(low), 열두 걸음에서 멈춘다 — 파일 열 개를 훑어야 하는 일에는
   * 셋 다 틀린 값이다. 실제로 그렇게 돌아 본 턴이 있고, 같은 질문을 깊게 본
   * 쪽과 견주면 답의 깊이가 눈에 띄게 달랐다.
   *
   * 여기 모은 낱말은 하나같이 **읽어야 답이 나오는** 말이다. 잘못 고르면
   * 파일을 못 고쳐서 사람이 막히므로, 문턱은 읽기 전용답게 높인다.
   */
  inspect: [
    [/점검(해|\s*좀|을|이)/, 5], [/분석(해|\s*좀|을|이)/, 5],
    [/검토(해|\s*좀|를)/, 5], [/리뷰(해|\s*좀)?/, 5],
    // '감사합니다' 는 안 걸린다 — '합' 은 '해·하' 가 아니다.
    [/감사(를)?\s*(해|하)/, 4], [/훑어\s*(봐|보고|줘)/, 4],
    /*
     * 「살펴보다」 는 영어 `inspect` 와 같은 뜻인데 점수표에 없었다. 영어
     * 쪽은 5점이라 "Inspect this code." 는 inspect 로 가고 "이 코드 살펴봐
     * 줘" 는 종합에 남았다 — 한국어를 먼저 쓰는 도구에서 방향이 거꾸로다.
     *
     * 앞에 「원인」 이 붙으면 뺀다. 그건 고장을 쫓는 말이라 debug 자리고,
     * 여기로 보내면 파일을 못 고치는 모드에서 원인만 듣고 끝난다.
     *
     * ── 토씨 목록에 한 짝의 절반만 적었다 ──────────────────────────
     *
     * `[을이은는]` 이라 「이유를」 「이유가」 가 빠졌다. 목적격·주격 토씨는
     * 받침 있는 말에 을·이, 없는 말에 를·가 가 붙는 **한 짝**인데 앞짝만
     * 적었다. '이유' 는 받침이 없어서 사람이 쓰는 꼴이 언제나 뒷짝이다 —
     *
     *   "원인을 살펴봐줘"   → debug   (빠진다)
     *   "이유를 살펴봐줘"   → inspect (안 빠진다)
     *
     * 같은 말인데 낱말 하나로 갈렸고, 하필 더 흔한 쪽이 **파일을 못 고치는**
     * 모드로 갔다.
     */
    [/(?<!(?:원인|까닭|이유)[을를이가은는]?\s{0,2}(?:좀\s{0,2})?)살펴\s*(봐|보고|보자|봐야|주)/, 5],
    [/취약(점|한|성)/, 5], [/보안\s*(점검|검토|문제|취약|구멍)/, 5],
    [/경합|레이스\s*컨디션|race\s*condition/i, 5],
    [/데이터\s*(손실|유실)|덮어쓰(기|면)|유실/, 4],
    [/동시\s*(편집|접근|요청|쓰기|성)/, 4],
    [/충돌/, 3], [/불일치|어긋/, 3], [/누락(된|되는|이|을)/, 3],
    [/위험(한|이|을|해)/, 3], [/결함/, 3],
    [/가능성(이|을)?\s*있는/, 3],
    [/문제(가|를|점)?\s*(있|찾|없|만)/, 3],
    [/찾아\s*(줘|주세요|봐|보)/, 2],
    /*
     * ── 읽기 쪽 영어도 같은 무게로 ──────────────────────────────────
     *
     * 쓰기 쪽 낱말 구멍(위 code 표)을 메우고도 **읽기 쪽은 그대로였다.**
     * 같은 뜻을 한국어로 하면 여기로 오고 영어로 하면 종합에 남았다 —
     * 여덟 짝을 재 보니 다섯이 어긋났다.
     *
     *     "이 코드 검토해 줘"      → inspect
     *     "Review this code."      → 종합
     *
     * 까닭은 무게였다. 한국어 '점검·분석·검토·리뷰·취약' 은 낱낱이 5점
     * 규칙인데, 영어는 다섯 낱말이 **4점 한 덩이**로 묶여 있었다. 문턱에
     * 하나 모자라고, 여럿이 겹쳐도 한 번만 세어진다.
     *
     * 그래서 한국어와 같은 모양으로 쪼개고 같은 무게를 준다. `audit` 만
     * 4점인 것은 한국어 '감사' 가 4점이라 그렇다 — 짝을 맞춘 것이다.
     */
    /*
     * 활용형까지 받는다. `\b` 로 딱 닫아 두니 `Analyzing` · `reviews` ·
     * `inspection` 이 0점이었다(7차 리뷰). 한국어는 '분석해·분석을·분석이'
     * 를 다 받는데 영어만 원형 하나였다.
     *
     * `audit` 도 5점으로 올린다. 4점으로 둔 까닭을 「한국어 '감사' 가
     * 4점이라」 고 적었는데, 한국어 '감사' 가 낮은 것은 **인사말과 겹뜻**
     * 이어서다. 영어 audit 에는 그 겹뜻이 없다 — 엉뚱한 근거였다.
     */
    [/\breview(?:s|ing|ed)?\b|\binspect(?:s|ing|ion|ions|ed)?\b/i, 5],
    [/\banaly[sz]e(?:s|d)?\b|\banaly[sz]ing\b|\banalysis\b/i, 5],
    [/\bvulnerab/i, 5],
    // `npm audit fix` · `yarn audit fix` 는 고치라는 명령이다. 읽기 전용 점검으로 보내면 못 돌린다 (6회차 Gemini 길고르기6 b1).
    [/\baudit(?:s|ing|ed)?\b(?!\s+fix\b)/i, 5],
    [/\bdata\s*loss\b|\bdeadlock\b|\blost\s*update\b/i, 4],
  ],

  ask: [
    // 설명은 아무것도 안 바꾸는 모드라 잘못 고르면 답답해진다. 신호를 엄격히 본다.
    /*
     * ── 물음표 **뒤에** 한마디를 더 붙이면 물음이 아니게 됐다 ─────────
     *
     * 줄 끝(`$`)에 못박혀 있었다. 그런데 사람은 물음표로 말을 안 끝낸다 —
     *
     *   "이거 뭐야?"          → ask 5점 → 설명 모드
     *   "이거 뭐야? 알려줘"   → ask 3점 → 종합 모드
     *
     * 「알려줘」 를 붙였더니 물음이 아닌 것이 됐다. 붙인 말은 **더 설명해
     * 달라는 말**인데 점수가 되레 깎였다(5 → 3). 남은 '알려 줘' 3점은
     * 문턱 5에 모자라서 종합으로 떨어졌다.
     *
     * 물음표가 있으면 그 자리가 문장 끝이다. 줄 끝이 아니어도 된다.
     * 물음표가 없을 때만 줄 끝을 따진다 — "뭐야 이게" 같은 말을 아무 데서나
     * 줍지 않으려는 것이 원래 못의 뜻이었고, 그건 그대로 둔다.
     */
    [/뭐(야|예요|입니까|지)\s*(?:\?|$)/m, 5], [/무엇(인가|입니까|이야)/, 5],
    /*
     * 「뭐 하는 파일이야?」 — 터미널에서 돌려 보다 걸린 자리다.
     *
     * 윗줄은 「뭐」 바로 뒤에 「야」 가 붙어야 걸린다. 그런데 사람이 실제로
     * 치는 말은 「이거 뭐 하는 파일이야?」 처럼 **사이에 말이 낀다.** 그래서
     * 제일 흔한 물음 하나가 통째로 안 걸렸고, 종합 모드에 그대로 남았다.
     * 막히지는 않지만 물어본 것에 답만 하면 되는 턴이 파일을 고칠 수 있는
     * 모드로 돌았다는 뜻이다.
     */
    [/뭐\s*하는\s*(파일|함수|코드|모듈|클래스|녀석|건|거|것)/, 5],
    [/뭐\s*하(는|나|니|냐)/, 3], [/무슨\s*(일|역할)/, 3],
    // 「설명만 해줘」 — 설명 뒤 「만」 을 안 받아 0점이었다. 설명만 달라는 말은 가장 뚜렷한 묻기다 (6회차 Gemini 길고르기6 b4).
    [/설명(해|\s*좀|을|만)/, 5], [/알려\s*(줘|주세요|줄래)/, 3],
    [/어떻게\s*(동작|작동|돌아가)/, 5],
    /*
     * 「어떻게 …?」 는 영어 `how to` 와 같은 물음인데 '동작·작동·돌아가'
     * 셋만 알고 있었다. 쓰는 법을 묻는 말이 통째로 0점이었다.
     *
     * 손대는 낱말은 안 넣는다 — "어떻게 고쳐?" 는 고쳐 달라는 말이지
     * 설명해 달라는 말이 아니다.
     */
    [/어떻게\s*(쓰|사용|실행|돌리|켜|끄|설정|설치|연결|고르|정하|불러|읽)/, 5],
    [/무슨\s*(뜻|의미)|의미(가|는)\s*뭐/, 5],
    [/차이(가|는|점)/, 3],
    [/궁금/, 3], [/왜\s*이렇게\s*(만들|짰|되어)/, 3],
    /*
     * 여기도 마찬가지다. 한국어 '설명·뭐야·무엇·어떻게 동작' 이 5점인데
     * 영어는 세 낱말이 4점 한 덩이였다. 짝을 맞춰 쪼갠다.
     *
     * 묻는 말꼴도 넓힌다 — `what is` 만 알고 `what does … do` 를 몰라서
     * "What does this function do?" 가 0점이었다. 한국어 '뭐 하는 거야'
     * 는 5점으로 아는 말이다.
     */
    /*
     * ── 묻는 말은 **문장 첫머리**에 있어야 묻는 말이다 ────────────────
     *
     * 넓히자마자 반대쪽이 샜다(7차 리뷰).
     *
     *     "Fix what is broken in route.js"  → ask 5점
     *
     * 고치라는 말이 물음과 비겨서 종합에 떨어졌다. 한국어 '뭐야' 규칙은
     * 줄 끝(`\s*\??$`)에 못박혀 있어서 이런 일이 없다 — 영어도 첫머리로
     * 못박는다. 물음표로 끝나는 문장은 낱말을 안 가리고 다 받는다.
     *
     * `explain` 은 첫머리가 아니어도 된다 — "Explain how …" 처럼 그 자체가
     * 시킴말이고, 한국어 '설명해' 도 자리를 안 가린다.
     *
     * ── 「말해 줘」 부류에는 첫머리 못이 있어야 한다 ──────────────────
     *
     * 9차 리뷰. `tell me` · `show me` · `walk me through` 를 자리 안 가리고
     * 넣었더니, 쓰기 지시문의 딸린 마디가 물음이 됐다.
     *
     *     "Rewrite this function to show me the result."   → ask 5점
     *     "Write the docs and tell me what changed."       → ask 5점
     *
     * `explain` 과 다르다. `explain` 은 그 자체가 문장의 시킴말이라 어디
     * 있든 묻는 말인데, 이 셋은 **딸린 마디에 더 자주 온다.** 그래서 못을 박는다.
     *
     * 이름씨 `explanation` 은 아예 뺀다. 8차에 사소 하나(「이름씨꼴이
     * 없다」)를 메우려고 넣었는데, 그 값으로 `"Add an explanation to the
     * README."` 가 물음이 됐다 — 사소를 메우려다 심각을 냈다. 첫머리에
     * 못을 박으면 정작 쓸모 있는 "Give an explanation of …" 가 죽고,
     * 안 박으면 위가 샌다. 얻는 것보다 잃는 것이 크면 안 넣는 것이 맞다.
     *
     * ── 못박아 놓고 한쪽만 못박혔다 ──────────────────────────────────
     *
     * 8차 리뷰. `how` 규칙을 `(첫머리)how…\?|how (to|does|…)` 로 적었다.
     * 세로줄은 **규칙 전체를 가른다.** 그래서 둘째 갈래에는 첫머리 못이
     * 아예 안 박혔고, 바로 위 주석이 막았다는 그 자리가 그대로 열려 있었다.
     *
     *     "Rewrite how to build it."   → ask 5점, **ask 모드**
     *
     * 고치라는 말이 **파일을 못 고치는 모드**로 갔다. 7차에 막은 것보다
     * 한 걸음 더 나쁘다 — 그때는 비겨서 종합에 떨어졌을 뿐이었다.
     * 갈래를 괄호로 묶어 못을 둘 다에 박는다.
     *
     * 첫머리에 `^\s*` 를 준다. 쓰기 갈래는 이미 그런데 여기만 안 그래서,
     * 빈칸 두 칸으로 시작하는 물음이 0점이었다.
     *
     * ── 이름 안의 마침표는 문장 끝이 아니다 ──────────────────────────
     *
     *     "What parameters does route.js take?"   → 0점
     *
     * 물음표 갈래를 `[^.!?\n]*\?` 로 적어서 `route.js` 의 마침표에서
     * 끊겼다. 마침표 뒤에 낱말 글자가 붙어 있으면 그건 문장 끝이 아니라
     * 이름의 일부다 — 이 파일 위쪽 영어시킴말이 이미 아는 이야기다.
     *
     * ── 묻는 꼴 세 가지가 더 있었다 ──────────────────────────────────
     *
     *   · `What <이름씨> does …`  보조동사가 `what` 바로 뒤에만 오는 줄
     *     알아서, 물음표가 없으면 통째로 0점이었다.
     *   · `How's …`  `what` 에는 준 줄임꼴을 `how` 에는 안 줬다.
     *   · 거꾸로 `how` 에만 준 `to` · `can` · `should` 를 `what` 에는 안
     *     줬다(9차 리뷰). "What to run next" · "What should I run first" 가
     *     물음표 없이는 0점이었다.
     *   · 이름 안 마침표 예외를 `\.(?=\w)` 로 적어 유닉스 상대 경로
     *     (`./route.js`)가 빠졌다. 슬래시도 낱말 글자로 친다.
     *   · `explanation`  `inspect` 는 이름씨꼴까지 받는데 여기만 안 받았다.
     *
     * 그리고 `Tell me how …` 는 첫머리 못 때문에 0점인데, 이건 자리
     * 문제가 아니라 **낱말 문제**다. 「말해 줘」 는 `explain` 과 같은 부류의
     * 물음 시킴말이라 그쪽에 넣는다 — 못을 뽑으면 위의 `Rewrite how to` 가
     * 도로 열린다.
     *
     * ── 공손한 한마디가 못 앞에 끼어들었다 ──────────────────────────────
     *
     * 첫머리 못을 `^\s*` 로 박아 놓으니 **빈칸 말고는 아무것도** 앞에 못
     * 온다. 그런데 영어로 묻는 사람은 그 자리에 거의 항상 한마디를 붙인다 —
     *
     *   "tell me what this does"          → ask 5점
     *   "Please tell me what this does"   → 전부 0점 → 종합 모드
     *   "Could you tell me what this does" → 전부 0점 → 종합 모드
     *
     * 공손하게 물을수록 물음으로 안 읽혔다. 답만 하면 되는 턴이 파일을
     * 고칠 수 있는 모드로 돌았다는 뜻이다.
     *
     * 그래서 못 바로 뒤에 **공손말 한 마디**를 허락한다. 못을 뽑는 것이
     * 아니다 — "Rewrite this function to show me the result." 은 여전히
     * 0점이다. 그 자리의 `to show` 앞에는 공손말이 아니라 딴 말이 있다.
     *
     * 한 마디가 아니라 **두 마디**가 붙기도 한다. "Please could you tell
     * me …" 는 실제로 흔한데, 세로줄로 갈라 놓으면 둘 중 하나만 먹고
     * 나머지가 남아 통째로 0점이 됐다. 그래서 갈래가 아니라 차례로 둔다.
     *
     * `what` · `how` 쪽에는 `please` 만 준다. 「Could you what …」 은 영어에
     * 없는 꼴이라 받아 봐야 걸릴 자리가 없다.
     *
     * ── 물어보고 **나서 고치라**는 한 문장 ───────────────────────────
     *
     *     "Please show me the config and rename it to config.json."
     *     "Could you tell me what changed and update the changelog?"
     *
     * 공손말을 받아 주자 이것들이 5점을 꽉 채워 **묻기 모드**로 갔다. 묻기는
     * 파일을 못 고치는 모드라, 사람은 설명만 받고 이름은 그대로다. 공손말을
     * 받기 전에는 0점이라 종합(고칠 수 있는 자리)에 남아 있었으니, 이 변경이
     * 없던 자리를 만든 셈이다.
     *
     * `and`·`then` 으로 이은 시킴 마디가 같은 문장에 있으면 이 규칙을 끈다.
     * 「and why」 「and how it works」 처럼 움직씨가 아닌 것은 안 건드린다.
     */
    [/(?:\bexplain(?:s|ing|ed)?\b|(?:^\s*|[.!?]\s+|\n\s*)(?:(?:please|pls)\s*,?\s+)?(?:(?:could|can|would|will)\s+you\s+)?(?:please\s*,?\s+)?(?:(?:tell|show)\s+(?:me|us)\b|walk\s+(?:me|us)\s+through\b))(?!(?:[^.!?\n]|\.(?=[\w/]))*\b(?:and|then)\s+(?:also\s+)?(?:implement|add|fix|write|rename|update|delete|remove|create|build|make|refactor|rewrite)\b)/im, 5],
    /*
     * ── 물음표를 삼킨 뒤에서는 이미 지나온 자리를 못 본다 ────────────────
     *
     * 위 explain 과 같은 문을 `what`·`how` 에도 달아 뒀는데, 물음표까지 삼키는
     * 갈래가 이기면 그 문이 **삼킨 자리 뒤**에서 열린다 — 이미 지나온
     * 「and fix」 에 닿을 수가 없었다. 물음표 하나로 이렇게 갈렸다 —
     *
     *     "What is broken and fix it."   → 막힌다
     *     "What is broken and fix it?"   → 묻기 5점, 묻기 모드
     *
     * 그래서 낱말 바로 뒤에도 같은 문을 단다. 뒤의 문도 그대로 둔다 —
     * 「What is this? Then fix it.」 은 앞의 문이 물음표에서 멈춰 못 보는
     * 자리라, 둘이 서로 다른 데를 지킨다.
     */
    [/(?:^\s*|[.!?]\s+|\n\s*)(?:(?:please|pls)\s*,?\s+)?what\b(?!(?:[^.!?\n]|\.(?=[\w/]))*\b(?:and|then)\s+(?:also\s+)?(?:implement|add|fix|write|rename|update|delete|remove|create|build|make|refactor|rewrite)\b)(?:(?:[^.!?\n]|\.(?=[\w/]))*\?|(?:'s|(?:\s+\w+){0,3}\s+(?:is|are|was|were|does|do|did|can|should)|\s+to)\b)(?!(?:[^.!?\n]|\.(?=[\w/]))*\b(?:and|then)\s+(?:also\s+)?(?:implement|add|fix|write|rename|update|delete|remove|create|build|make|refactor|rewrite)\b)/im, 5],
    [/(?:^\s*|[.!?]\s+|\n\s*)(?:(?:please|pls)\s*,?\s+)?how\b(?!(?:[^.!?\n]|\.(?=[\w/]))*\b(?:and|then)\s+(?:also\s+)?(?:implement|add|fix|write|rename|update|delete|remove|create|build|make|refactor|rewrite)\b)(?:(?:[^.!?\n]|\.(?=[\w/]))*\?|'s\b|\s+(?:to\b|does|do|did|is|are|was|were|can|should)\b)(?!(?:[^.!?\n]|\.(?=[\w/]))*\b(?:and|then)\s+(?:also\s+)?(?:implement|add|fix|write|rename|update|delete|remove|create|build|make|refactor|rewrite)\b)/im, 5],
    [/읽어(만)?\s*보고\s*(설명|알려)/, 4],
  ],

  orchestrator: [
    // 총괄의 신호는 낱말 하나가 아니라 '넓이' 다. 그래서 하나하나는 약하게 두고
    // 여럿이 겹칠 때 문턱을 넘게 한다 — 실제로 큰 일에는 이런 말이 같이 나온다.
    // "전체 로그 형식을 다 통일해줘" 는 '전체' 와 '통일' 이 겹쳐야 총괄이 된다.
    [/전부|모두|싹\s*다|통째로/, 3],
    [/전체/, 3],
    [/하나씩/, 3],
    [/끝까지/, 3], [/마무리까지/, 3],
    [/여러\s*(개|군데|파일|단계|곳)/, 3],
    [/순서대로/, 3],
    [/통일(해|시켜|\s*좀)|일괄/, 2],
    [/흩어진|여기저기/, 2],
    [/\bend[- ]?to[- ]?end\b/i, 3],
  ],

  code: [
    // 「고쳐주지 마」(부정) · 「고쳐지지 않는」(입음)은 고치라는 말이 아니다 — 여태 4점으로 code 문턱을 넘었다 (6회차 Gemini 길고르기6 c1).
    [/고쳐(?!지|\s*주?지\s*(?:마|말|않))|수정(해|\s*좀)/, 4],
    /*
     * ── 도움움직씨 앞은 띄어 쓴다 ──────────────────────────────────
     *
     * 「넣어 주세요」 「써 줘」 는 맞춤법대로 띄어 쓴 꼴인데 붙여 쓴 것만
     * 봤다. 그래서 같은 부탁이 이렇게 갈렸다 —
     *
     *   "로그 한 줄 넣어줘"    → code
     *   "로그 한 줄 넣어 줘"   → 종합 (0점)
     *
     * 아래 손대라는말 이 「수정해 줘」 에서 이미 배운 것인데, 점수표만
     * 그대로 남아 있었다. 띄어 쓴 사람만 시킨 것이 안 읽혔다.
     */
    [/만들어|추가(해|\s*좀)|넣어\s*(줘|주세요)/, 4],
    [/구현(해|\s*좀)/, 5],
    [/바꿔(줘|주세요)?|변경(해|\s*좀)/, 3],
    [/지워(줘)?|삭제(해|\s*좀)/, 3],
    /*
     * 영어 `rename` 은 여기서 뺐다. 첫머리 갈래가 이미 5점을 주므로
     * `Rename the file.` 만 8점(5+3)이 되어, 5차에서 고친 동사 어긋남이
     * 여기 하나 남아 있었다. `Rename failed - file in use` 도 3점을 받아
     * 로그가 code 문턱에 닿았다(6차 리뷰). 문장 가운데 갈래로 옮긴다.
     */
    [/이름\s*바꿔|리네임/, 3],
    [/옮겨(줘)?/, 2],
    [/리팩터(링)?(해|\s*좀)/, 3],
    /*
     * 첫머리 시킴말. 한국어의 '구현해'(5점)와 같은 무게다 — 같은 말을
     * 어느 말로 했느냐로 모드가 갈리면 안 된다.
     *
     * 증거가 세서 세게 준다. 이 규칙은 낱말 하나를 보는 것이 아니라
     * **문장 첫머리 + 골라 둔 동사 + 실패말이 안 따라옴**을 다 요구한다.
     * `error` 같은 맨낱말(3점)보다 훨씬 좁게 잡힌 규칙이다.
     *
     * 4점이었을 때 남은 자리가 있었다. 설계 낱말이 하나도 없는 긴 명세에서
     * 시킴말 하나(4)가 지나가는 `error`·`failed` 둘(6)에게 졌다.
     * 5점이면 1·2등이 붙고, 원래 있던 「비기면 안 고른다」 규칙이 받아 낸다 —
     * 새 울타리를 세우지 않고 눈금만 맞추는 것이 이 자리의 고침이다.
     */
    [영어시킴말, 5],
    /*
     * 문장 가운데의 시킴말. 첫머리 갈래(5점)와 **겹치지 않게** 앞을 막는다.
     * 안 막았더니 `Fix the web app.` 은 7점(5+2)이고 `Build the web app.` 은
     * 5점이었다 — 같은 뜻인데 어느 동사를 골랐느냐로 점수가 갈렸다. 긴
     * 명세에서는 그 2점이 모드를 바꾼다(하나는 code, 하나는 종합).
     *
     * 그래서 **앞에 낱말이 있어야** 이 규칙이 돈다. `please` 는 첫머리
     * 갈래가 이미 세는 말이라 따로 뺀다.
     *
     * 빼는 자리에도 쉼표를 받는다. 첫머리 갈래가 `Please, fix …` 를 세게
     * 되자 여기가 또 2점을 얹어 `Please, build …`(5) 와 갈렸다 —
     * 겹침을 막는 자리는 겹치는 쪽과 **같은 꼴**을 봐야 한다.
     */
    [/(?<=[a-z,;:)\]]\s{1,3})(?<!\b(?:could|can|would|will)\s{1,3}(?:you|we)[\s,]{1,4})(?<!\bplease[\s,]{1,4})(?:implement|add|fix|write|rename)\b/i, 2],
    [/써\s*(줘|주세요)/, 3],
    /*
     * 「짜다」 는 코드·검사를 만들라는 한국어에서 제일 흔한 말인데
     * '써 줘' 만 있었다. 계획·순서·구조를 짜는 것은 각각 plan·architect
     * 규칙이 이미 더 세게 잡으므로 여기서 따로 뺄 것이 없다.
     */
    [/짜\s?(줘|주세요|봐|보자)/, 3],
  ],
};

// 이만큼은 되어야 바꾼다. 못 미치면 종합 모드 그대로 둔다.
//
// 읽기 전용 모드는 문턱을 높인다. 잘못 보내면 파일을 못 고쳐서 사용자가 막힌다.
// 파일을 고칠 수 있는 모드는 잘못 골라도 일이 막히지는 않으므로 낮게 둔다.
const 문턱 = {
  debug: 3,
  code: 3,
  orchestrator: 4,
  plan: 5,
  architect: 5,
  inspect: 5,
  ask: 5,
};

/*
 * ── 겹친 요청 ───────────────────────────────────────────────────────────
 *
 * "계획해주고 만들어줘" 처럼 **계획과 실행이 한 말에 같이** 든 것.
 *
 * 이건 위의 점수표로는 못 고른다. '만들어' 가 code 3점 문턱을 그냥 넘어서
 * 곧장 코드 모드로 가 버리고, 사람이 보자고 한 계획은 안 나온다.
 * 실제로 겪었다 — "정리해서 만들어줘" 가 계획 한 줄 없이 파일부터 만들었다.
 *
 * 반대로 plan 으로 보내면 계획만 내고 멈춘다. 그것도 시킨 일의 절반이다.
 * 그래서 따로 알아본다. 계획을 내고 → 승인을 받고 → 그대로 실행까지 잇는다.
 *
 * 좁게 잡는다. 사용자는 자율 실행을 원했고, 승인 창은 그 결정을 갉아먹는
 * 쪽이다. '고쳐줘' 한마디에 창이 뜨면 그게 더 나쁘다.
 * 그래서 **계획말 + 잇는말** 이 앞에 있고 그 **뒤에** 실행말이 와야만 겹침이다.
 */
/*
 * '알아보' 가 아니라 '알아' 다. 「알아보고」 는 '알아' + '보고' 로 갈라야
 * 아래 잇는말의 '보고' 와 이어진다. '알아보' 로 묶어 두면 남는 것이 '고'
 * 하나뿐이라 어떤 잇는말과도 안 맞았다 — 아래 머리말 참고.
 */
const 계획말 = '계획|플랜|설계|구상|기획|정리|검토|조사|파악|살펴|알아|분석|로드맵|방향|순서|밑그림|초안';
/*
 * '정리하고' 처럼 잇는 꼴이어야 한다. 그냥 '정리' 하나로는 겹침이 아니다 —
 * "코드 정리해줘" 는 계획이 아니라 그 자체가 할 일이다.
 *
 * ── 한국말에서 가장 흔한 이음꼴이 빠져 있었다 ───────────────────────────
 *
 * 「…해 보고」 다. 「살펴보고」 「알아보고」 「검토해 보고」 — 계획을 먼저
 * 하고 그다음에 하라는 말을 사람들이 실제로 제일 많이 쓰는 꼴인데, 목록에
 * 없었다. 그래서 이 규칙이 잡으라고 만들어진 바로 그 문장이 안 잡혔다 —
 *
 *   "살펴보고 만들어줘"   → 겹침 아님 → code 모드 → 계획 한 줄 없이 파일부터
 *   "정리하고 만들어줘"   → 겹침      → 계획을 보여 주고 승인을 받음
 *
 * 같은 말인데 이음꼴 하나로 갈렸다. 앞의 사람은 「먼저 보고 나서」 라고
 * 적어 놓고도 계획을 못 봤다.
 *
 * ── 「보고」 를 이름씨와 가르는 자리 ────────────────────────────────────
 *
 * 처음엔 `보고(?![서를가])` 하나로 막았다. 「검토 보고서를 만들어줘」 는
 * 겹침이 아니라 그냥 할 일이니 맞는 방향이었는데, **가르는 자리가 틀렸다.**
 * 「살펴보고서 만들어줘」 의 '-고서' 는 이름씨가 아니라 멀쩡한 이음씨끝인데
 * 같이 막혔다. 뒤에 뭐가 오느냐만 봐서는 둘을 못 가른다.
 *
 * 앞을 봐야 갈린다 — 그런데 「빈칸이 있나」 가 아니라 **앞말이 무엇이냐** 다.
 *
 *   "살펴보고서 만들어줘"   — 앞이 움직씨 줄기(살펴) → 이음말
 *   "검토보고서 작성해줘"   — 앞이 이름씨(검토) → '보고서' 라는 한 낱말
 *
 * 처음엔 빈칸만 보고 갈랐다. 붙어 있으면 이음말로 쳤더니 「검토보고서」
 * 「분석보고서」 가 통째로 겹침이 됐다 — 보고서를 써 달라는 말이 계획 모드로
 * 가서 계획만 내고 멈춘다. 빈칸은 우연이고, 갈리는 것은 앞말의 품사다.
 *
 * '살펴·알아' 는 움직씨 줄기라 뒤에 오는 '보고' 가 언제나 도움움직씨다.
 * '검토·분석·조사' 는 이름씨라 '보고' 가 붙으면 한 낱말이 된다. 이름씨 쪽은
 * '해' 를 끼워야 도움움직씨가 된다 — 「검토해 보고」. 그 꼴에는 뒷말을
 * 따진다(「검토해 보고서를」 은 다시 이름씨다).
 */
const 이름씨아님 = '(?![서를가에의는도만까와과])';
const 잇는말 = '해\\s*주고|해\\s*놓고|하고\\s*나서|하고서|한\\s*다음|한\\s*뒤|한\\s*후|하고|해서|잡고|짜고|세우고|낸\\s*뒤|낸\\s*다음'
  + `|(?<=(?:살펴|알아)\\s{0,1})보고|해\\s*보고${이름씨아님}`
  + '|(?<=(?:살펴|알아)\\s{0,1})본\\s*(?:다음|뒤|후)|해\\s*본\\s*(?:다음|뒤|후)';
/*
 * 이름씨 뿌리(구현·작성·생성·세팅·설치·배포)는 **해·하·좀** 이 붙어야 실행말이다. 맨 뿌리로 두었더니
 * 「살펴보고 작성자 확인해줘」 의 작성자, 「검토하고 설치 경로 알려줘」 의 설치 가 걸려 조회가 겹친 요청
 * (계획 모드 + 승인 창)이 됐다 (6회차 Gemini 길고르기6 c3). 사이에 토씨 하나(을·은·도·까지·만)는 넘는다 —
 * 「구현은 해줘」 「배포까지 해줘」.
 */
const 실행말 = /만들어|만들자|만들어라|(?:구현|작성|생성|세팅|설치|배포)(?=\s*(?:을|를|은|는|도|까지|만)?\s*(?:해|하|좀))|추가(해|하)|써\s*(줘|주세요)|짜\s*(줘|주세요)|고쳐/;
const 계획먼저 = new RegExp(`(${계획말})\\s*(?:을|를)?\\s*(?:${잇는말})`);
/*
 * 영어도 같이 본다 — "plan and then build it" 처럼 붙여 넣는 일이 있다.
 *
 * ── 쉼표를 이음말로 세던 것 ────────────────────────────────────────────
 *
 * 이음말 목록에 `,` 가 들어 있었다. 그런데 영어에서 쉼표로 이어진 동사들은
 * **차례가 아니라 열거**다.
 *
 *   "independently design, implement, test, verify, and complete …"
 *
 * 이건 「설계하고 나서 만들어라」 가 아니라 「이것들을 다 해라」 다. 그런데
 * `design` 과 `implement` 가 예순 자 안에 있으니 겹침으로 잡혔고, 겹침이면
 * **계획 모드 + 승인 창**으로 간다 — 계획만 내고 멈춘다.
 *
 * 실제로 겪었다. 「Do not stop after producing a plan」 이라고 **명시적으로
 * 적어 둔** 지시문이 계획 모드로 갔다. 시킨 것과 정반대다.
 *
 * 그래서 쉼표를 뺀다. 차례를 뜻하는 말이 있을 때만 겹침이다. 한국어 쪽은
 * 처음부터 잇는말 목록으로 이렇게 하고 있었고, 영어 쪽만 헐거웠다.
 *
 * ── 쉼표를 목록에서만 뺐지, 지나가는 길은 안 막았다 ─────────────────────
 *
 * 위를 그렇게 고쳐 놓고도 **바로 그 예문이 여전히 겹침으로 잡혔다.**
 *
 *   "independently design, implement, test, and build the feature"
 *
 * 이음말 목록에서 `,` 를 뺀 것은 맞다. 그런데 목록에 남아 있던 맨 `and` 가
 * `design` 에서 예순 자 떨어진 **맨 끝의 `, and build`** 와 짝지어졌다.
 * 사이의 쉼표들은 `[\s\S]{0,60}?` 가 그냥 지나갔다. 열거를 차례로 읽는
 * 결과는 처음과 똑같았다 — 계획 모드로 가서 계획만 내고 멈춘다.
 *
 * 그래서 길을 나눈다.
 *
 *   - `and then` · `then` · `after that` · `before you` 처럼 **차례를 적어
 *     둔 말**이면 사이에 쉼표가 있어도 겹침이다 ("Plan the migration,
 *     then implement it" — 사람이 차례라고 썼다).
 *   - 맨 `and` 하나로 잇는 것은 **쉼표를 하나까지만 넘는다** ("Design and
 *     implement" 과 "Plan the migration, and implement it" 은 겹침,
 *     "design, implement, test, and build" 는 열거).
 *
 * 쉼표 하나를 남겨 둔 까닭은, 영어에서 `and` 앞에 쉼표를 찍는 것이 그냥
 * 흔해서다. 열거와 갈리는 자리는 **쉼표가 몇 개냐** 다 — 열거는 `and` 에
 * 닿기까지 쉼표를 여러 번 넘어야 하고, 차례는 많아야 한 번이다.
 *
 * ── 차례말 **뒤**에도 쉼표가 붙는다 ────────────────────────────────────
 *
 * 위는 차례말 **앞**의 쉼표 이야기였다. 정작 흔한 것은 뒤에 붙는 쪽이다 —
 *
 *   "Plan the migration. After approval, implement it."
 *   "Draft the schema. Then, write the migration."
 *
 * 차례말 다음을 `\s+` 로만 받아서, 쉼표 하나에 겹침이 아니게 됐다. 사람이
 * 「승인 뒤에」 라고 **적어 둔** 자리인데 계획을 보여 달라는 절반이 통째로
 * 사라진다. 앞에서 받아 준 것과 같은 쉼표라 같이 받는다.
 *
 * ── `the design` 은 계획을 세우라는 말이 아니다 ─────────────────────────
 *
 * 이 네 낱말은 움직씨이기도 하고 이름씨이기도 하다. 앞에 관사가 붙으면
 * 이름씨다 — 그때는 계획을 세우라는 말이 아니라 그냥 무엇을 가리키는 말이다.
 *
 *   "The design is fine. Run the linter and write the changelog."
 *   "Our plan is approved. Open the file and make the change."
 *
 * 둘 다 겹침으로 잡혀 계획 모드로 갔다. 뒤의 것은 사람이 **이미 승인됐다고
 * 적어 놨는데** 또 계획을 내고 승인을 물었다.
 *
 * 맨 `and` 갈래는 문장 끝도 못 넘게 한다. 쉼표보다 센 구분자인데 `[^,]` 가
 * 마침표·물음표·줄바꿈을 다 지나갔다. 그리고 `\s*` 는 상한이 없어서 예순 자
 * 잣대가 빈칸으로 뚫렸다 — `[ \t]*` 로 좁힌다.
 */
/*
 * ── 건너뛰라는 말 · 이미 끝났다는 말 ─────────────────────────────────────
 *
 * 「Skip design and write the code」 · 「Plan is approved, then implement it」 이 겹침으로 잡혀 계획 모드
 * (읽기 전용 + 승인 창)로 갔다. 앞은 설계를 **하지 말라**는 말이고, 뒤는 계획이 **이미 승인됐다**는
 * 말이다 — 둘 다 곧장 만들라는 말인데 계획서를 또 내고 또 물었다 (6회차 Gemini 길고르기6 c4).
 * 관사를 막은 것과 같은 까닭이다: 앞에 skip·no·without 이 오거나 뒤에 is·was·has 가 오면 그 낱말은
 * 시킴이 아니다.
 */
const 영어겹침 = /(?<!\b(?:the|a|an|this|that|our|your|my|its|their)\s)(?<!\b(?:skip|skipping|skipped|no|without|forget|drop)\s+)\b(plan|design|outline|draft)\b(?!\s+(?:is|was|are|were|has|have|had|looks?|seems?|got)\b)(?:[\s\S]{0,60}?\b(?:and\s+then|then|after\s+(?:that|approval)|before\s+you)|[^,.!?\n]{0,60}?,?[ \t]{0,2}\band)\s*,?\s+(?:then\s+)?\b(build|implement|create|write|make|code)\b/i;

/*
 * ── 두 말이 **얼마나 붙어 있어야** 겹침인가 ─────────────────────────────
 *
 * 예순 자다. 영어 쪽은 처음부터 `[\s\S]{0,60}?` 로 거리를 두고 봤는데
 * 한국어 쪽만 「그 뒤 어디든」 이었다. 그 차이가 긴 글에서 그대로 터졌다.
 *
 * 실제로 겪은 것 — 1,300자짜리 지시문이었다. 「…배포 전 점검한다고 가정하고
 * 전체적으로 검토해줘 … 실제로 개선하는 것이다 … 코드 수정만 수행해라 …
 * 중간에 멈춰서 확인을 요청하지 말고 스스로 진행해라」.
 *
 * 여기서 800자 떨어진 두 조각 — 「검토해서」(9번째 문단)와 「추가하」(보고
 * 항목 이름) — 이 짝지어져 겹침으로 잡혔다. 그래서 **계획 모드**로 갔다.
 * 계획은 파일을 바꾸는 도구가 없는 모드이고, 그 지시문은 「고쳐라 · 테스트를
 * 만들어라 · 멈추지 마라」 였다. 사람은 고쳐 달라고 했는데 계획서 한 장을
 * 받고, 마지막에 「이대로 진행할까요?」 를 봤다 — 묻지 말라고 적어 둔 바로
 * 그 자리에서.
 *
 * 이 규칙이 잡으려던 것은 「정리해서 만들어줘」 같은 **한 문장**이다. 한 문장
 * 안에서 두 말은 예순 자를 안 넘는다. 그러니 거리를 재는 것이 규칙을 좁히는
 * 것이 아니라 **원래 뜻대로 되돌리는 것**이다.
 */
const 겹침거리 = 60;

/*
 * ── 「묻지 말고 끝까지」 라고 이미 적어 둔 사람 ─────────────────────────
 *
 * 겹침의 값은 「계획을 보여 주고 승인을 받는다」 다. 그런데 승인을 받지
 * 말라고 **글로 적어 둔** 사람에게 그 값은 손해뿐이다. 계획 모드는 계획을
 * 내고 멈추므로, 그 사람은 시킨 일의 절반도 못 받는다.
 *
 * 이건 짐작이 아니라 사람이 직접 쓴 말이다. 쓴 말이 규칙을 이긴다.
 */
const 멈추지마 = /(?:중간에\s*)?(?:멈추지|묻지|물어보지|확인\s*요청하지|되묻지)\s*(?:는)?\s*(?:말|마)|끝까지\s*(?:수행|진행|해)|스스로\s*진행|알아서\s*(?:진행|해)|승인\s*없이|\bwithout\s+(?:asking|stopping|confirmation)\b|\bdo\s*n[o']?t\s+(?:stop|ask|pause|halt)\b|\bcontinue\s+autonomously\b|\bproceed\s+autonomously\b|\bwithout\s+(?:pausing|waiting|approval)\b/i;

/** 사람이 「묻지 말고 끝까지 하라」 고 적어 뒀나. */
export function 묻지말라했나(text) {
  return 멈추지마.test(String(text ?? ''));
}

/**
 * 계획과 실행이 한 말에 같이 들었나.
 *
 * @returns {{겹침: boolean, why: string}} 겹침이 아니면 why 는 빈 글.
 */
export function 겹친요청(text) {
  const s = String(text ?? '');
  // 묻지 말라고 적어 둔 사람에게 승인 창을 띄우지 않는다. 위 머리말 참고.
  if (묻지말라했나(s)) return { 겹침: false, why: '' };

  const 영어 = s.match(영어겹침);
  if (영어) return { 겹침: true, why: `'${영어[1]}' 과 '${영어[2]}'` };

  const 앞 = s.match(계획먼저);
  if (!앞) return { 겹침: false, why: '' };
  /*
   * 실행말이 계획말 **바로 뒤에** 와야 한다.
   *
   * 「뒤에」 였지 「글 어디든」 이 아니었다 — 거리를 안 재면 긴 지시문에서는
   * 언제나 짝이 하나쯤 걸린다(위 겹침거리 머리말). "만들고 나서 정리해줘" 가
   * 안 걸리는 것도 그대로다. 그건 만드는 일이 먼저고, 미리 볼 계획이 없다.
   */
  const 남은것 = s.slice(앞.index + 앞[0].length, 앞.index + 앞[0].length + 겹침거리);
  const 뒤 = 남은것.match(실행말);
  if (!뒤) return { 겹침: false, why: '' };
  /*
   * 실행말 **바로 뒤에 부정**이 붙으면 실행하라는 말이 아니다. 「설계하고 구현은 하지 마」 「정리하고
   * 추가하지 마」 가 겹침으로 잡혀 계획 모드 + 승인 창으로 갔다 — 하지 말라는 것을 할지 물었다
   * (6회차 Gemini 길고르기6 c2).
   *
   * ── 부정은 **잘린 토막**이 아니라 글 전체에서 읽는다 ──────────────────────────
   *
   * 실행말은 예순 자 안에서 찾는 것이 맞다(위 겹침거리 머리말). 그런데 그 뒤에 오는
   * 부정까지 같은 토막 안에서만 찾았다. 실행말이 토막 가장자리에 걸리면 「하지 마」 가
   * 통째로 잘려 나가고, 하지 말라는 것을 할지 묻는 승인 창이 떴다 —
   *
   *   "설계하고 … 정리해 두되 구현은 하지 마"   ← 구현이 쉰아홉 자째
   *
   * 거리를 재는 것은 **두 말이 한 문장 안인가**를 보려는 것이고, 부정은 이미 찾은
   * 실행말에 바로 붙은 것만 본다 — 잣대가 다르니 재는 자리도 다르다. 글 전체에서 읽는다.
   *
   * ── 「하지는 마」 · 「하진 마」 · 「하지도 마」 ─────────────────────────────────
   *
   * '지' 와 '마' 사이에 토씨가 끼거나(하지는·하지도) 줄어들면(하진) 못 읽었다. 하지
   * 말라는 말이 하라는 말이 되는 자리라 뒤집힘이 제일 크다. 「하지만」 은 그대로 안
   * 걸린다 — 뒤에 오는 것이 마·말·않 일 때만 부정이다.
   */
  const 실행말뒤 = s.slice(앞.index + 앞[0].length + 뒤.index + 뒤[0].length);
  if (/^\s*(?:(?:은|는|을|를|도)\s*)?(?:(?:해\s*주|해|하|주)\s*)?(?:지[는도를은]?|진)\s*(?:마|말|않)/.test(실행말뒤)) return { 겹침: false, why: '' };
  return { 겹침: true, why: `'${앞[0].trim()}' 뒤에 '${뒤[0].trim()}'` };
}

/*
 * ── 손대라는 말 ─────────────────────────────────────────────────────────
 *
 * "폴더 구조 개선해줘" 가 설계 모드로 갔다. 설계는 **읽기만 하는 모드**라,
 * 사람은 고쳐 달라고 했는데 설계안 한 장을 받고 끝났다. 이 파일 맨 위에
 * 적어 둔 것이 정확히 그 경우다 — "읽기 전용 모드로 잘못 보내면 사용자는
 * '왜 파일을 안 고쳐?' 하고 막힌다."
 *
 * 낱말 점수로는 못 막는다. '구조' 와 '개선' 이 둘 다 설계의 강한 신호라서,
 * 뒤에 붙은 '해줘' 를 아무리 봐도 점수로는 안 뒤집힌다.
 *
 * 그래서 **동사를 따로 본다.** 손대라는 말이 있으면 읽기만 하는 모드
 * (설계·계획·묻기)는 후보에서 뺀다. 남은 것 중에 고르고, 없으면 종합에
 * 그대로 둔다 — 종합은 파일을 고칠 수 있다.
 *
 * 가르는 자리가 미묘하다. '설명해줘' 와 '개선해줘' 는 둘 다 '…해줘' 지만
 * 하나는 읽고 말하라는 말이고 하나는 고치라는 말이다. 그래서 어미가 아니라
 * **동사 목록**으로 본다. 목록에 없으면 손대라는 말이 아니다.
 */
export const 손대는동사 = '만들|고쳐|고치|바꿔|바꾸|추가|넣어|지워|삭제|옮겨|나눠|나누|합쳐|정리|개선|구현|작성|적용|설치|배포|리팩터링|리팩터|분리|통일|세팅|갈아|손봐|손보|수정';
/*
 * 동사 바로 뒤에 시킴꼴이 붙어야 한다. 빈칸을 안 넘으므로 "나누는 설계만 봐줘"
 * 는 안 걸린다 — 그건 나누라는 말이 아니라 나누는 안을 보자는 말이다.
 *
 * ── 해라체가 통째로 빠져 있었다 ─────────────────────────────────────────
 *
 * 끝맺음 목록이 전부 **부탁하는 말**이었다(줘 · 주세요 · 해라 · 할래). 그래서
 * 시키는 말투로 쓴 사람은 이 검사에 한 번도 안 걸렸다 —
 *
 *   「재현 검사를 먼저 만들어라 … 최소 범위의 코드 수정만 수행해라
 *     … 중간에 멈추지 말고 스스로 진행해라」
 *
 * 이 지시문이 「고치라는 말이 아니다」 로 읽혀서 읽기 전용 모드로 갔다.
 * 사람은 고쳐 달라고 세 번 적었는데 아무것도 안 바뀐 화면을 받는다.
 *
 * 「어라 · 아라」 와 맨 「라」 가 그 문을 연다. 맨 라 는 낱말 끝일 때만 본다 —
 * 「지워라」 는 지우라는 말이지만 「바꾸라는 말」 은 아니다. 뒤에 무엇이
 * 붙어 있나로 그 둘이 갈린다.
 * 「만들어라」 는 언제나 만들라는 말이다.
 */
/*
 * ── 여기 백슬래시 하나가 빠져 있었다 ────────────────────────────────────
 *
 * 이 무늬는 **템플릿 글**로 짓는다(동사 목록을 끼워 넣어야 해서). 그런데
 * 템플릿 글 안에서 `\s` 는 정규식에 닿기 전에 자바스크립트가 먼저 먹는다 —
 * 모르는 이스케이프라 백슬래시를 떼고 **글자 `s`** 만 남긴다.
 *
 *     적은 것   ...|라(?=[\s.!?~,]|$))
 *     닿은 것   ...|라(?=[s.!?~,]|$))     ← 빈칸이 아니라 알파벳 s
 *
 * 그래서 맨 「라」 뒤에 **빈칸이나 줄바꿈이 오면 안 걸렸다.** 걸리는 것은
 * 「라」 가 글 맨 끝이거나 바로 뒤에 . ! ? ~ , 가 붙을 때뿐이었다.
 *
 *     "그것 좀 바꿔라"        걸림   (글 끝)
 *     "바꿔라 지금 해줘"      안 걸림 ← 여러 마디로 시키면 거의 다 이쪽이다
 *     "지워라 그리고 저장해"  안 걸림
 *
 * 위 주석이 「사람은 고쳐 달라고 세 번 적었는데 아무것도 안 바뀐 화면을
 * 받는다」 고 적어 둔 바로 그 고장이, 고쳐 놓고도 백슬래시 하나 때문에
 * 그대로 남아 있었다. `\\s` 로 적어야 정규식이 `\s` 를 받는다.
 *
 * (`\\S{0,3}` 는 처음부터 두 개라 멀쩡했다. 한 무늬 안에서 한쪽만 틀렸으니
 *  눈으로는 더 안 보인다.)
 */
/*
 * ── 띄어쓰기 한 칸이 시킴말을 통째로 숨겼다 ─────────────────────────────
 *
 * 사이를 `\S{0,3}` 로 적어서 **빈칸을 하나도 못 넘었다.** 그런데 「수정해 줘」
 * 「고쳐 줘」 는 맞춤법대로 띄어 쓴 꼴이다. 그래서 같은 말이 이렇게 갈렸다 —
 *
 *   "이 구조를 살펴보고 코드 수정해줘"   → code 모드
 *   "이 구조를 살펴보고 코드 수정해 줘"  → 종합 (손대라는 말이 아닌 것으로 읽힘)
 *
 * 모드만 갈린 것이 아니다. 이 검사는 「하라고 한 일을 두고 무슨 일을 할지
 * 되묻지 않는다」 는 자리(askcheck.js)에도 쓰인다. 띄어 쓴 사람은 그 그물이
 * 통째로 안 걸려서, 시켜 놓고도 「무슨 일을 할까요?」 를 다시 받았다.
 *
 * 세 글자 상한은 그대로다. 그래서 "나누는 설계만 봐줘" 는 여전히 안 걸린다 —
 * 「나누」 와 「줘」 사이가 일곱 자다.
 */
/*
 * ── 「계획 정리해 줘」 는 계획을 다시 세워 달라는 말이다 ────────────────
 *
 * 영어 쪽에는 이 예외가 처음부터 있었다. `Update the plan.` 은 시킴말로 안
 * 센다 — 고칠 파일 이름이 아니라 **계획 그 자체**가 목적어라서, 계획 모드로
 * 가는 것이 맞다. 그런데 한국어 쪽에는 그 예외가 없었다.
 *
 *   "이전 계획 정리해 줘"   → 종합
 *   "Update the plan."      → 계획
 *
 * 같은 말이 언어에 따라 다른 데로 갔다. 한국어로 물은 사람만 계획을 못 받았다.
 *
 * 목적어가 계획거리일 때만, 그리고 **계획을 손질하는 뜻의 동사**일 때만
 * 뺀다. 영어가 그은 선과 같은 자리다 — `Implement the plan we agreed on.`
 * 은 영어에서도 시킴말이다. 계획을 실행하라는 말은 손대라는 말이 맞다.
 * 그래서 '구현·추가·삭제…' 는 그대로 두고 '만들·작성·바꾸·정리·수정' 만 뺀다.
 *
 * 「계획서」 「plan.md」 는 안 걸린다. 그건 계획거리가 아니라 **파일**이고,
 * 파일을 고치라는 말은 고치라는 말이다. 영어가 `plan.md` 를 가르는 방법
 * (`\.(?!\w)`)과 같은 뜻이다.
 *
 * 목록을 손대는동사 에서 갈라 내므로, 새 동사를 넣으면 자동으로 「예외 없는」
 * 쪽에 들어간다 — 예외로 뺀 것은 '만들·작성·바꾸(바꿔)·정리·수정' 다섯 뜻이다.
 *
 * 계획거리와 동사 사이에 '좀' 이 끼는 것도 받는다. 「계획 좀 수정해 줘」 는
 * 「계획 수정해 줘」 와 같은 말인데, 한 글자 때문에 딴 데로 가면 안 된다.
 */
/*
 * 계획거리는 plan 점수표에 있는 것만 적는다. '기획' 을 넣어 봤더니 예외는
 * 걸리는데 갈 데가 없었다 — plan 점수가 0이라 「손대라는 말이 아니다」 까지만
 * 가고 결국 종합이나 코드로 떨어졌다. 빼는 자리와 보내는 자리가 다르면
 * 어느 쪽도 제 일을 못 한다.
 *
 * 사이에 끼는 말도 받는다. 「계획 다시 정리해 줘」 「계획 좀 더 정리해줘」 는
 * 「계획 정리해 줘」 와 같은 말이다. 아무 말이나 받으면 「계획 파일 수정해 줘」
 * 까지 빠지므로, 끼워도 되는 것은 **어찌씨 몇 개**로 못박는다.
 */
const 계획거리 = '계획|플랜|로드맵';
const 끼는말 = '좀|더|다시|새로|전체|모두|싹|한번';
const 계획손질 = new Set(['만들', '작성', '바꿔', '바꾸', '정리', '수정']);
const 동사들 = 손대는동사.split('|');
const 계획도손대는동사 = 동사들.filter((v) => 계획손질.has(v)).join('|');
const 계획말고손대는동사 = 동사들.filter((v) => !계획손질.has(v)).join('|');
const 계획거리뒤 = `(?<!(?:${계획거리})[을를]?\\s{0,3}(?:(?:${끼는말})\\s{0,3}){0,2})`;
/*
 * ── 빈칸을 넘어도 되는 어미와 안 되는 어미 ──────────────────────────────
 *
 * 「빈칸을 하나도 못 넘는다」 를 고치면서 사이를 `[^\n]{0,3}` 으로 열었다.
 * 그런데 **어미마다 빈칸 사정이 다르다.**
 *
 * 빈칸을 넘어야 하는 것은 도움움직씨 '주다' 뿐이다 — 「고쳐 줘」 「고쳐
 * 주세요」 는 맞춤법대로 띄어 쓴 꼴이다. '하자·해라·어라' 같은 씨끝은
 * 줄기에서 절대 안 떨어진다.
 *
 * 그래서 전부에 빈칸을 열어 줬더니 이런 것이 걸렸다 —
 *
 *   "추가 하자 있는 부분 알려줘"   → 손대라는 말
 *
 * '하자' 가 씨끝이 아니라 **이름씨(결함)** 인데, 앞의 '추가' 와 빈칸으로만
 * 떨어져 있어서 「추가하자」 로 읽혔다. 묻는 말이 고치라는 말이 됐다.
 *
 * ── 「고치지 마라」 는 고치라는 말이 아니다 ─────────────────────────────
 *
 * 이건 빈칸을 열기 **전부터** 있던 구멍이다(붙여 쓴 「고치지마라」 가 그때도
 * 걸렸다). 사이가 세 글자라 '지 마' 가 통째로 들어앉고, 남은 '라' 가 시킴꼴
 * 로 읽힌다. 하지 말라는 말이 하라는 말이 되는 것이라 뒤집힘이 제일 크다.
 */
/*
 * ── 맨 「라」 는 시킴꼴이기도 하고 서술격조사이기도 하다 ─────────────────
 *
 *   "임시 파일 지워라"                    — 해라체 시킴꼴
 *   "이걸 개선이라 볼 수 있는지 검토해 줘"  — 「개선이다」 의 '-이라'
 *
 * 뒤엣것이 시킴말로 읽혀서, 검토해 달라는 물음이 「고치라는 말」 이 됐다.
 * 그러면 점검 모드가 후보에서 빠지고 화면에는 **거꾸로 된 까닭**이 뜬다 —
 * 「고치라는 말이라 읽기 전용 모드로 안 보냄」. 고치라고 한 적이 없는데.
 *
 * 가르는 자리는 줄기의 종류다. '개선·구현·배포' 같은 한자어는 시킴꼴이
 * 언제나 '…해라' 라 맨 '라' 가 붙을 일이 없다 — 붙었으면 그건 '-이라' 다.
 * '지워·바꿔·고쳐' 같은 토박이 줄기만 맨 '라' 를 쓴다.
 */
const 해붙는동사 = new Set(['추가', '삭제', '정리', '개선', '구현', '작성', '적용',
  '설치', '배포', '리팩터링', '리팩터', '분리', '통일', '세팅', '수정']);
const 해붙는것 = 동사들.filter((v) => 해붙는동사.has(v)).join('|');
const 맨라 = `${해붙는것 ? `(?<!(?:${해붙는것})이?)` : ''}라(?=[\\s.!?~,]|$)`;
const 붙는끝 = `해라|하라|하자|해다오|할래|어라|아라|여라|${맨라}`;
const 띄는끝 = '줘|주세요|주라|줄래';
const 하지마 = '(?!\\S{0,2}지\\s*마|\\S{0,2}지\\s*말)';
/*
 * 빈 갈래를 안 만든다. 위 두 목록은 손대는동사 를 갈라 낸 것이라, 목록을
 * 손대다가 한쪽이 비면 `(?:)` 가 되고 **빈 글에 걸린다** — 그러면 모든 말이
 * 손대라는 말이 되어 읽기 전용 모드가 통째로 사라진다. 아무 데서도 안 터지고
 * 화면에도 안 나오는 꼴이라, 만들지 않는 쪽으로 막아 둔다.
 */
const 손대라는말 = new RegExp(`(?:${[
  계획말고손대는동사 && `(?:${계획말고손대는동사})`,
  계획도손대는동사 && `${계획거리뒤}(?:${계획도손대는동사})`,
].filter(Boolean).join('|')})${하지마}(?:\\S{0,3}(?:${붙는끝})|[^\\n]{0,3}(?:${띄는끝}))`);

/*
 * ── 반말로 시킨 말이 통째로 빠져 있었다 ─────────────────────────────────
 *
 * 끝맺음 목록에 해라체(해라·어라)와 부탁(줘·주세요)은 있는데, 제일 흔한
 * **반말 시킴꼴**(「수정해」 「고쳐」 「만들어」)이 없었다. 그래서 같은 말이
 * 이렇게 갈렸다 —
 *
 *   "리뷰 결과 반영해서 정리해"      → inspect (읽기 전용 — 고칠 수가 없다)
 *   "보안 점검 결과대로 수정해"      → 종합   ("…수정해줘" 는 code)
 *   "리뷰 결과대로 고쳐"             → 종합   (inspect 5 · code 4 비슷함)
 *
 * 반말은 서술·물음과 꼴이 같아서 넓게 열면 안 된다. 그래서 세 겹으로 좁힌다 —
 *   (1) 동사 바로 뒤에 반말꼴이 **정확히** 붙고(「수정해」 「고쳐」), 그 뒤가
 *       글 끝·줄 끝·마침표뿐일 때만 — 「고쳐졌어」 「수정해도」 「구현해 본 적」
 *       「수정해?」 는 안 걸린다.
 *   (2) 바로 앞이 「안·못」 이면 아니다 — 「안 고쳐」 는 안 고친다는 말이다.
 *   (3) 같은 문장 앞쪽에 물음말(어떻게·왜·뭘…)이 있으면 아니다 — 「이 에러
 *       어떻게 수정해」 는 물음표를 안 쳤을 뿐 묻는 말이다.
 * 계획거리 예외(「계획 좀 수정해」)는 위 무늬와 같은 자리에 같이 건다.
 * (6회차 Gemini 길고르기6d 지적을 실행으로 확인.)
 */
const 반말꼴 = (v) => (해붙는동사.has(v) ? `${v}해` : v === '만들' ? '만들어' : /[어아워쳐꿔겨눠봐]$/.test(v) ? v : null);
const 반말로 = (목록) => 목록.map(반말꼴).filter(Boolean).join('|');
const 계획말고반말 = 반말로(동사들.filter((v) => !계획손질.has(v)));
const 계획도반말 = 반말로(동사들.filter((v) => 계획손질.has(v)));
const 반말앞 = '(?<!(?:안|못)\\s?)(?<!(?:어떻게|왜|뭘|뭐|무엇을|어디를?|언제|어느)[^.!?\\n]{0,20})';
const 반말끝맺음 = '(?=[.!~]*[ \\t]*(?:\\n|$)|[.!~]+\\s)';
const 반말시킴 = new RegExp(`${반말앞}(?:${[
  계획말고반말 && `(?:${계획말고반말})`,
  계획도반말 && `${계획거리뒤}(?:${계획도반말})`,
].filter(Boolean).join('|')})${반말끝맺음}`);

// 파일을 못 고치는 모드. modes.js 에서 쓰기 도구를 안 주는 것들과 같아야 한다.
export const 읽기만하는모드 = new Set(['architect', 'plan', 'ask', 'inspect']);

/*
 * ── 영어로 시킨 말은 하나도 못 보고 있었다 ──────────────────────────────
 *
 * 위 규칙은 어미(`줘` · `해라` …)로 시킴꼴을 가린다. 영어에는 그 어미가 없다.
 * 그래서 "Build a complete web application." 이 **손대라는 말이 아닌 것**으로
 * 읽혔고, 읽기 전용 모드(설계·계획·점검·묻기)가 후보에서 안 빠졌다.
 *
 * 영어는 **문장 첫머리의 명령형**으로 가린다. 그래야 시키는 말과 설명하는
 * 말이 갈린다 —
 *
 *   "Build a work request app."          → 손대라는 말이다
 *   "Explain how the build works."       → 아니다 (build 가 첫머리가 아니다)
 *   "The build system is broken."        → 아니다
 *
 * 목록에 없는 동사는 손대라는 말이 아니다. 한국어 쪽과 같은 규칙이다 —
 * 넓히면 「설명해 줘」 가 고치라는 말이 되고, 그러면 읽기 전용 모드가 영영
 * 안 골라진다.
 */
/*
 * 정규식 리터럴로 적는다. 글자열을 이어 붙여 new RegExp 에 넣으면 홑따옴표
 * 안에서 `\s` 의 역슬래시가 사라지고 `\b` 는 백스페이스 글자가 된다 —
 * 검사에서 잡히기 전까지 조용히 아무것도 안 맞는다. 실제로 그렇게 났다.
 */
// 위에서 세운 공용 무늘 그대로다. 두 벌로 두면 한쪽만 늘어난다.
const 영어손대라 = 영어시킴말;

/** 파일을 손대라는 말이 들었나. */
export function 손대라했나(text) {
  const s = String(text ?? '');
  return 손대라는말.test(s) || 반말시킴.test(s) || 영어손대라.test(s);
}

/*
 * ── 승인받을 사람이 없는 자리 ───────────────────────────────────────────
 *
 * 겹침의 값은 「계획을 보여 주고 **승인을 받아** 그대로 잇는다」 다. 그 값은
 * 승인할 사람이 있어야 생긴다.
 *
 * `deel run` 은 물어볼 사람이 없는 자리다(그 검사 파일의 머리글이 바로 「물어볼
 * 사람이 없는 자리에서 서지 않고 끝나는가」 다). 거기서 겹침을 계획 모드로
 * 보내면 계획을 한 장 찍고 끝난다 — 계획 모드는 파일을 고치는 도구가 없다.
 * 승인 창은 뜰 자리가 없고, 이어 갈 턴도 없다. 시킨 일의 절반도 못 준다.
 *
 * 그래서 부르는 쪽이 「여기는 승인받을 수 있는 자리인가」 를 말한다. 없다고
 * 하면 겹침 갈래를 건너뛰고 점수표대로 고른다 — 「정리해서 만들어줘」 는
 * 코드 모드로 가서 정리도 하고 만들기도 한다.
 *
 * 겹쳤다는 사실 자체는 그대로 돌려준다. 부르는 쪽이 「계획도 같이 달라는
 * 말이었다」 고 한 줄 적을 수 있어야 한다.
 */

/**
 * 이 한마디가 무슨 일인지 고른다.
 *
 * @param {string} text 사용자가 친 말
 * @param {{승인받을수있나?: boolean}} [어떻게] 승인받을수있나 가 false 면
 *        겹친 요청을 계획 모드로 안 보낸다 (물어볼 사람이 없는 자리).
 * @returns {{ mode: string|null, score: number, why: string, 겹침: boolean, 점수들: object }}
 *          mode 가 null 이면 '모르겠다' 는 뜻이다 — 종합 모드에 그대로 둔다.
 *          겹침이 true 면 계획을 내고 승인을 받아 실행까지 이어야 한다는 뜻이다.
 */
export function route(text, { 승인받을수있나 = true } = {}) {
  const s = String(text ?? '');

  // 겹친 요청은 점수표보다 먼저 본다. 점수표에 맡기면 '만들어' 가 이겨서
  // 계획을 건너뛴다 — 그게 바로 이 길을 만든 이유다.
  const 겹 = 겹친요청(s);
  if (겹.겹침 && 승인받을수있나) return { mode: 'plan', score: 0, why: 겹.why, 겹침: true, 점수들: {} };

  const 점수들 = {};
  const 근거 = {};

  for (const [mode, 규칙들] of Object.entries(표)) {
    let 합 = 0;
    const 걸린것 = [];
    for (const [re, 점] of 규칙들) {
      const m = s.match(re);
      if (!m) continue;
      합 += 점;
      걸린것.push(m[0].trim());
    }
    점수들[mode] = 합;
    근거[mode] = 걸린것;
  }

  /*
   * 손대라는 말이 있으면 읽기만 하는 모드는 후보에서 뺀다.
   *
   * 점수를 깎지 않고 **줄에서 뺀다.** 깎으면 얼마나 깎을지가 또 하나의 눈금이
   * 되고, 그 눈금은 아무도 못 지킨다. 여기서 가르는 것은 정도의 문제가 아니라
   * 종류의 문제다 — 고칠 수 있는 자리인가 아닌가.
   */
  /*
   * 승인받을 자리가 없는데 겹친 요청이면 여기서도 뺀다.
   *
   * 겹쳤다는 것은 「실행하라는 절반이 분명히 있다」 는 뜻이다. 그런데 물어볼
   * 사람이 없으면 계획 모드는 계획 한 장으로 끝난다. 「plan and then build
   * it」 이 그랬다 — 겹침 갈래를 건너뛰어도 점수표의 `plan` 5점이 이겨서
   * 그대로 읽기 전용 모드로 갔다. 건너뛰는 것만으로는 모자란다.
   */
  const 손대라했다 = 손대라했나(s);
  const 승인자리없다 = 겹.겹침 && !승인받을수있나;
  const 손대라 = 손대라했다 || 승인자리없다;
  // 왜 뺐는지는 화면에 그대로 뜬다. 두 까닭은 사람에게 아주 다른 말이라,
  // 하나로 뭉뚱그리면 「고치라고 한 적 없는데」 하고 막힌다.
  const 뺀까닭 = 손대라했다 ? '고치라는 말이라' : '승인받을 자리가 없어서';
  const 뺀것 = [];
  let 순위 = Object.entries(점수들).sort((a, b) => b[1] - a[1]);
  if (손대라) {
    for (const [m, n] of 순위) if (읽기만하는모드.has(m) && n > 0) 뺀것.push(m);
    순위 = 순위.filter(([m]) => !읽기만하는모드.has(m));
  }
  /*
   * ── 긴 명세에서는 한 번 걸린 낱말이 결정하면 안 된다 ──────────────────
   *
   * 점수표는 **한마디**를 보라고 만든 것이다. 「이거 왜 안 되지」 처럼 짧은
   * 말에서는 낱말 하나가 곧 뜻이다.
   *
   * 그런데 5,000자짜리 명세를 붙여 넣으면 이야기가 달라진다. 그런 글에는
   * 모든 모드의 낱말이 한 번씩은 나온다 — 설계도 나오고, 검사도 나오고,
   * 보안도 나온다. 거기서 한 번 걸린 것은 **뜻이 아니라 소음**이다.
   *
   * 실제로 겪었다. 「빈 폴더에서 웹 앱을 처음부터 만들어라」 는 5,254자
   * 지시문이 `fails` 한 낱말 때문에 디버그로 갔다. 그 글에 그 낱말은
   * 「검사가 실패하면 원인을 조사하라」 는 조건절에 딱 한 번 있었다.
   *
   * 그래서 글이 길면 **더 뚜렷할 때만** 고른다. 못 고르면 종합에 그대로
   * 두는데, 긴 명세에는 그게 오히려 맞는 답이다 — 종합은 걸음마다 단계를
   * 따라가므로(agent/phase.js) 계획에서 시작해 만들기로 알아서 넘어간다.
   */
  const 긴글 = s.length >= 1500;
  const 더줘야 = 긴글 ? 2 : 0;

  const [으뜸, 점수] = 순위[0] ?? [null, 0];

  /*
   * ── 눈금을 올리는 것으로는 두 번째 낱말을 못 막는다 ────────────────────
   *
   * 위의 +2 는 **낱말 하나**를 막는다. 실제 벤치마크 지시문을 그대로 넣어
   * 보니 두 개가 걸렸다 — 둘 다 증상이 아니라 요구사항 조건절이었다.
   *
   *   "… handle duplicate requests, stale requests, unexpected server errors."  +3
   *   "… A failed operation must not leave state silently inconsistent."        +3
   *
   * 합이 6이라 3+2 를 넘어 디버그가 됐다. 고장 난 것이 하나도 없는 **빈
   * 폴더**에서 시작하라는 글인데, 디버그 모드는 「증상을 한 문장으로 다시
   * 적어라 · 재현 방법부터 확보해라」 를 시킨다. 없는 증상을 찾게 된다.
   *
   * 눈금을 또 올려도 세 낱말이면 그대로 뚫린다. 눈금은 글 길이만 보고
   * 정하는데, 지나가는 낱말은 길이가 아니라 **글의 종류**를 따라 늘기
   * 때문이다. 그래서 눈금이 아니라 견줌으로 본다.
   *
   * 같은 글에서 설계가 8점, 디버그가 6점이었다. 「고치라는 말이라 설계로는
   * 안 보낸다」 는 그대로 맞다 — 그런데 그것은 **설계로 보내지 말라**는 말
   * 이지, 그 글이 디버그라는 말이 아니다. 뺀 것이 남은 으뜸보다 세다면 이
   * 글이 무엇인지는 아직 안 정해진 것이다. 종합에 둔다 — 종합은 걸음마다
   * 단계를 따라가므로(agent/phase.js) 계획에서 시작해 만들기로 넘어간다.
   *
   * 짧은 말에는 안 건다. 짧은 말에서는 낱말 하나가 곧 뜻이다.
   */
  const 뺀최고 = Math.max(0, ...뺀것.map((m) => 점수들[m] ?? 0));
  /*
   * 동점도 지나가는 말이다. 「뺀 것이 남은 으뜸보다 세면 안 정해진 것」 이라 적어 놓고 `<` 로 재서, 설계 10 ·
   * 디버그 10 인 긴 글이 디버그로 갔다 — 같은 세기면 더더욱 안 정해진 것이다. 그리고 남은 으뜸이 **0점**이면
   * 그건 으뜸이 아니다: 화면이 「architect(10점) 가 debug(0점) 보다 세서 debug 로 안 봄」 이라고, 신호가
   * 하나도 없던 모드를 견준 것처럼 적었다 (6회차 Gemini 길고르기6 e3·e4). 0점이면 아래 「신호가 있었지만
   * 고치라는 말이라」 갈래로 간다.
   */
  const 지나가는말 = 긴글 && Boolean(으뜸) && 점수 > 0 && 점수 <= 뺀최고;

  // 골랐어야 할 만한 신호가 있었는데 일부러 안 보낸 자리인가.
  const 일부러 = Boolean(지나가는말 || 뺀것.length
    || (긴글 && 으뜸 && 점수 >= (문턱[으뜸] ?? 4)));

  if (!으뜸 || 지나가는말 || 점수 < (문턱[으뜸] ?? 4) + 더줘야) {
    // 왜 안 바꿨는지는 화면에 그대로 뜬다. "설계로 갈 뻔했는데 고치라는 말이라
    // 안 보냈다" 와 "그냥 모르겠다" 는 사람에게 아주 다른 말이다.
    const 왜 = 지나가는말
      ? `글이 길고 ${뺀것.join('·')}(${뺀최고}점) 가 ${으뜸}(${점수}점) 보다 세서 `
        + `${으뜸} 로 안 봄 — 종합에서 단계를 따라갑니다`
      : 긴글 && 으뜸 && 점수 >= (문턱[으뜸] ?? 4)
        ? `글이 길어 ${으뜸} 신호 하나로는 안 정함 — 종합에서 단계를 따라갑니다`
        : 뺀것.length
          ? `${뺀것.join('·')} 신호가 있었지만 ${뺀까닭} 읽기 전용 모드로 안 보냄`
          : '무슨 일인지 뚜렷하지 않음';
    /*
     * ── 「일부러 안 골랐다」 와 「그냥 모르겠다」 는 다른 말이다 ─────────
     *
     * 바로 위 주석이 그 구분을 적어 뒀는데, 정작 화면에는 **둘 다 아무것도
     * 안 떴다.** 부르는 쪽(repl.js · oneshot.js)이 `if (골라진.mode)` 안에서만
     * 찍었기 때문이다. 여기서 지은 세 갈래 까닭은 한 번도 사람에게 안 갔다.
     *
     * 그렇다고 전부 찍으면 안 된다. 보통 한마디는 거의 다 여기로 오고 까닭은
     * 「무슨 일인지 뚜렷하지 않음」 이다 — 그건 소음이다.
     *
     * 그래서 **가르는 일을 여기서 한다.** 부르는 쪽이 `뺀것.length` 니
     * `글이 길` 이니를 다시 헤아리게 두면 두 자리에서 따로 어긋난다.
     */
    return { mode: null, score: 점수, why: 왜, 겹침: 겹.겹침, 점수들, 뺀것, 일부러 };
  }

  // 1·2등이 붙으면 고르지 않는다. 반반인 것을 억지로 정하면 절반은 틀린다.
  const 둘째 = 순위[1]?.[1] ?? 0;
  if (점수 - 둘째 < 2 && 둘째 >= (문턱[순위[1][0]] ?? 4)) {
    return {
      mode: null, score: 점수, why: `${으뜸} 과 ${순위[1][0]} 가 비슷함`,
      겹침: 겹.겹침, 점수들, 뺀것, 일부러: true,
    };
  }

  return {
    mode: 으뜸,
    score: 점수,
    why: [근거[으뜸].slice(0, 2).map((x) => `'${x}'`).join(', '),
      뺀것.length ? `(${뺀것.join('·')} 는 ${뺀까닭} 뺐습니다)` : ''].filter(Boolean).join(' '),
    겹침: 겹.겹침,
    점수들,
  };
}
